---
phase: 17-description-template-coverage
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/__tests__/describe-route.test.ts
autonomous: true
requirements:
  - DESCR-01
  - DESCR-02
  - DESCR-03
  - DESCR-04
  - DESCR-05
  - DESCR-06
  - DESCR-07
  - DESCR-08

must_haves:
  truths:
    - "All 8 DESCR requirements have failing automated test coverage before any prompt is written"
    - "TRENCHER test now asserts absence (not.toContain) since the section is being removed"
    - "CRAWLER TRACTOR standalone test is updated to expect BULLDOZER/CRAWLER TRACTOR merged heading"
    - "JET SKI test is replaced by a PERSONAL WATERCRAFT test"
    - "Tests for all missing truck subtypes (crane_truck, fuel_truck, garbage, hook_bin, skip_bin, stock_truck, tanker, tray_truck, water_truck, coupe-truck) are written and RED"
    - "Tests for all 24 trailer subtype headings are written and RED"
    - "Tests for all new earthmoving subtypes are written and RED"
    - "Tests for all agriculture, forklift, caravan, and marine subtypes are written and RED"
  artifacts:
    - path: "src/__tests__/describe-route.test.ts"
      provides: "TDD scaffold — all new tests RED, all existing tests still GREEN or updated"
      contains: "PERSONAL WATERCRAFT"
  key_links:
    - from: "src/__tests__/describe-route.test.ts"
      to: "src/app/api/describe/route.ts"
      via: "getSystemContent() helper reads DESCRIPTION_SYSTEM_PROMPT"
      pattern: "systemContent\\.toContain\\("
---

<objective>
Write failing tests for every new and changed template section required in Phase 17. Tests must be RED (failing) before any prompt content is written — this is the TDD scaffold that plans 02-04 will make GREEN.

Purpose: Establish clear acceptance criteria as executable tests. Execution plans 02-04 are complete only when all tests in this file pass.
Output: Updated `src/__tests__/describe-route.test.ts` with tests RED against current prompt, covering all 8 DESCR requirements.
</objective>

<execution_context>
@/home/jack/.claude/get-shit-done/workflows/execute-plan.md
@/home/jack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-description-template-coverage/17-CONTEXT.md
@.planning/phases/17-description-template-coverage/17-RESEARCH.md
@.planning/phases/17-description-template-coverage/17-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update/add failing tests for DESCR-01 through DESCR-08</name>
  <files>src/__tests__/describe-route.test.ts</files>
  <read_first>
    - src/__tests__/describe-route.test.ts (full file — understand existing test structure, helpers, and current coverage before adding anything)
    - src/app/api/describe/route.ts (lines 1–352 — the DESCRIPTION_SYSTEM_PROMPT to understand what already exists vs what is missing)
  </read_first>
  <behavior>
    RED tests to add (these must fail against the current prompt):

    DESCR-01 — Truck gaps (add new `it()` blocks inside the existing truck describe block):
    - 'contains CRANE TRUCK section heading' → expect(s).toContain('CRANE TRUCK')
    - 'contains FUEL TRUCK section heading' → expect(s).toContain('FUEL TRUCK')
    - 'contains GARBAGE section heading' → expect(s).toContain('GARBAGE')
    - 'contains HOOK BIN section heading' → expect(s).toContain('HOOK BIN')
    - 'contains SKIP BIN section heading' → expect(s).toContain('SKIP BIN')
    - 'contains STOCK TRUCK section heading' → expect(s).toContain('STOCK TRUCK')
    - 'contains TANKER (TRUCK) section heading' → expect(s).toContain('TANKER (TRUCK)')
    - 'contains TRAY TRUCK section heading' → expect(s).toContain('TRAY TRUCK')
    - 'contains WATER TRUCK section heading' → expect(s).toContain('WATER TRUCK')
    - 'contains COUPE (TRUCK) section heading' → expect(s).toContain('COUPE (TRUCK)')

    DESCR-02 — Trailer distinct sections (replace existing generic TRAILER test or add new describe block):
    - 'contains FLAT DECK TRAILER section heading' → toContain('FLAT DECK TRAILER')
    - 'contains CURTAINSIDER TRAILER section heading' → toContain('CURTAINSIDER TRAILER')
    - 'contains PANTECH TRAILER section heading' → toContain('PANTECH TRAILER')
    - 'contains REFRIGERATED CURTAINSIDER (TRAILER) section heading' → toContain('REFRIGERATED CURTAINSIDER')
    - 'contains REFRIGERATED PANTECH (TRAILER) section heading' — existing, keep
    - 'contains LOW LOADER section heading' → toContain('LOW LOADER')
    - 'contains SIDE TIPPER section heading' → toContain('SIDE TIPPER')
    - 'contains TIPPER TRAILER section heading' → toContain('TIPPER TRAILER')
    - 'contains TANKER TRAILER section heading' → toContain('TANKER TRAILER')
    - 'contains TIMBER JINKER section heading' → toContain('TIMBER JINKER')
    - 'contains SKEL TRAILER section heading' → toContain('SKEL TRAILER')
    - 'contains STOCK TRAILER section heading' → toContain('STOCK TRAILER')
    - 'contains SIDE LOADER (TRAILER) section heading' → toContain('SIDE LOADER')
    - 'contains CAR CARRIER section heading' → toContain('CAR CARRIER')
    - 'contains DOG / PIG / TAG section heading' → toContain('DOG / PIG / TAG')
    - 'contains DOLLY section heading' → toContain('DOLLY')
    - 'contains PLANT TRAILER section heading' → toContain('PLANT TRAILER')
    - 'contains WALKING FLOOR TRAILER section heading' → toContain('WALKING FLOOR TRAILER')
    - 'contains BOX TRAILER section heading' → toContain('BOX TRAILER')
    - 'contains DECK WIDENER section heading' → toContain('DECK WIDENER')
    - 'contains COUPE TRAILER section heading' → toContain('COUPE TRAILER')

    DESCR-03 — Earthmoving merge:
    - UPDATE existing 'contains CRAWLER TRACTOR section heading' test (line ~772) → change to:
      'contains BULLDOZER/CRAWLER TRACTOR merged heading' → expect(s).toContain('BULLDOZER/CRAWLER TRACTOR')
    - UPDATE existing BULLDOZER test (line ~750) if it tests for a standalone BULLDOZER heading that conflicts — check and update to match merged heading if needed

    DESCR-04 — New earthmoving subtypes:
    - 'contains EARTHMOVING ATTACHMENTS section heading' → toContain('EARTHMOVING ATTACHMENTS')
    - 'contains CONVEYORS / STACKERS section heading' → toContain('CONVEYORS / STACKERS')
    - 'contains CRUSHER section heading' → toContain('CRUSHER')
    - 'contains MOTOR SCRAPER section heading' → toContain('MOTOR SCRAPER')
    - 'contains SCRAPER (PULL-TYPE) section heading' → toContain('SCRAPER (PULL-TYPE)')
    - 'contains SCREENER section heading' → toContain('SCREENER')
    - 'contains TRACKED LOADER section heading' → toContain('TRACKED LOADER')
    - 'contains TRACKED SKID STEER LOADER section heading' → toContain('TRACKED SKID STEER LOADER')
    - 'contains WASHING PLANT section heading' → toContain('WASHING PLANT')
    - 'contains COUPE (EARTHMOVING) section heading' → toContain('COUPE (EARTHMOVING)')
    - UPDATE TRENCHER test (line ~767): change from toContain('TRENCHER') to NOT.toContain('TRENCHER') — section is being removed

    DESCR-05 — Agriculture (new describe block):
    - 'contains TRACTOR section heading' → toContain('TRACTOR')
    - 'contains COMBINE HARVESTER section heading' → toContain('COMBINE HARVESTER')
    - 'contains AIR SEEDER section heading' → toContain('AIR SEEDER')
    - 'contains DISC SEEDER section heading' → toContain('DISC SEEDER')
    - 'contains SPRAY RIG / SPRAYER section heading' → toContain('SPRAY RIG / SPRAYER')
    - 'contains BALER section heading' → toContain('BALER')
    - 'contains MOWER / CONDITIONER section heading' → toContain('MOWER / CONDITIONER')
    - 'contains PLOUGH section heading' → toContain('PLOUGH')
    - 'contains GRAIN AUGER section heading' → toContain('GRAIN AUGER')
    - 'contains FORESTRY EQUIPMENT section heading' → toContain('FORESTRY EQUIPMENT')
    - 'contains OTHER AGRICULTURE section heading' → toContain('OTHER AGRICULTURE')
    - 'contains COUPE (AGRICULTURE) section heading' → toContain('COUPE (AGRICULTURE)')

    DESCR-06 — Forklift (new describe block):
    - 'contains FORKLIFT (CLEARVIEW MAST / CONTAINER MAST) section heading' → toContain('FORKLIFT (CLEARVIEW MAST / CONTAINER MAST)')
    - 'contains WALKIE STACKER section heading' → toContain('WALKIE STACKER')
    - 'contains ELECTRIC PALLET JACK section heading' → toContain('ELECTRIC PALLET JACK')
    - 'contains WALK BEHIND (PALLET JACK) section heading' → toContain('WALK BEHIND (PALLET JACK)')
    - 'contains STOCK PICKER / ORDER PICKER section heading' → toContain('STOCK PICKER / ORDER PICKER')
    - 'contains EWP (FORKLIFT-MOUNTED) section heading' → toContain('EWP (FORKLIFT-MOUNTED)')
    - 'contains OTHER FORKLIFT section heading' → toContain('OTHER FORKLIFT')
    - existing TELEHANDLER test — keep (already covered)

    DESCR-07 — Caravan (new describe block):
    - 'contains CAMPER TRAILER section heading' → toContain('CAMPER TRAILER')
    - 'contains MOTORHOME section heading' → toContain('MOTORHOME')
    - 'contains OTHER CARAVAN / CAMPER section heading' → toContain('OTHER CARAVAN / CAMPER')
    - 'contains COUPE (CARAVAN) section heading' → toContain('COUPE (CARAVAN)')
    - existing CARAVAN test — keep

    DESCR-08 — Marine (update/new):
    - UPDATE existing JET SKI test block (lines ~538–590): replace `toContain('JET SKI')` with `toContain('PERSONAL WATERCRAFT')` and update the distinctness check to `jetSkiIdx = systemContent.indexOf('PERSONAL WATERCRAFT')` / remove the marineIdx ordering check or update description to reflect new structure
    - 'contains MARINE (RECREATIONAL BOAT) section heading' → toContain('MARINE (RECREATIONAL BOAT)')
    - 'contains TRAILER BOAT section heading' → toContain('TRAILER BOAT')
    - 'contains BARGE section heading' → toContain('BARGE')
    - 'contains COMMERCIAL VESSEL section heading' → toContain('COMMERCIAL VESSEL')
    - 'contains FISHING VESSEL section heading' → toContain('FISHING VESSEL')
    - 'contains TUG / WORKBOAT section heading' → toContain('TUG / WORKBOAT')
    - 'contains OTHER MARINE VESSEL section heading' → toContain('OTHER MARINE VESSEL')
    - 'contains COUPE (MARINE) section heading' → toContain('COUPE (MARINE)')

    Test helper pattern to follow (existing in file — do not create a new helper):
    ```typescript
    async function getSystemContent(assetType: string, assetSubtype: string): Promise<string> {
      // reads from the mock — pattern already in file, do not reinvent
    }
    ```
  </behavior>
  <action>
    Read the full test file first. Identify the existing describe blocks and `getSystemContent` helper pattern. Then:

    1. Add new `it()` blocks inside existing describe blocks where a relevant block already exists (truck, earthmoving, marine). Create new `describe('Phase 17 — Agriculture templates', ...)`, `describe('Phase 17 — Forklift templates', ...)`, and `describe('Phase 17 — Caravan subtypes', ...)` blocks for first-time coverage. Add a `describe('Phase 17 — Trailer subtype coverage', ...)` block for all 24 trailer headings (replacing or supplementing the existing generic TRAILER test).

    2. Update in place (DO NOT delete, change the assertion):
       - CRAWLER TRACTOR test (~line 772): update to check for 'BULLDOZER/CRAWLER TRACTOR'
       - JET SKI test block (~lines 538–590): replace 'JET SKI' with 'PERSONAL WATERCRAFT' throughout the test
       - TRENCHER test (~line 767): change `toContain('TRENCHER')` to `not.toContain('TRENCHER')` — the section WILL be removed from the prompt in plan 02/03

    3. Do NOT add watch-mode flags. Do NOT change the test framework setup.

    4. After writing all tests, run:
       `npm run test -- --reporter=verbose src/__tests__/describe-route.test.ts`
       Confirm the NEW tests fail (RED) and existing non-updated tests remain GREEN. The updated TRENCHER, CRAWLER TRACTOR, and JET SKI tests will ALSO be RED against the current prompt — this is correct.

    5. Commit: `test(17-01): add failing tests for all DESCR-01 through DESCR-08 requirements`
  </action>
  <verify>
    <automated>npm run test -- --reporter=verbose src/__tests__/describe-route.test.ts 2>&1 | tail -30</automated>
  </verify>
  <done>
    - All newly added tests are failing (RED) against current DESCRIPTION_SYSTEM_PROMPT
    - Updated TRENCHER, CRAWLER TRACTOR, JET SKI tests are RED (expected — prompt has not been changed yet)
    - All pre-existing tests that were not updated are still GREEN
    - Test file committed with message: test(17-01): add failing tests for all DESCR-01 through DESCR-08 requirements
  </done>
  <acceptance_criteria>
    - grep -c 'CRANE TRUCK\|FUEL TRUCK\|GARBAGE\|HOOK BIN\|SKIP BIN\|STOCK TRUCK\|TRAY TRUCK\|WATER TRUCK' src/__tests__/describe-route.test.ts returns 8 or more matches
    - grep 'BULLDOZER/CRAWLER TRACTOR' src/__tests__/describe-route.test.ts returns at least 1 match
    - grep 'PERSONAL WATERCRAFT' src/__tests__/describe-route.test.ts returns at least 1 match
    - grep 'not.toContain.*TRENCHER\|TRENCHER.*not.toContain' src/__tests__/describe-route.test.ts OR grep -v 'toContain' with TRENCHER — verify TRENCHER test now asserts absence
    - grep 'COMBINE HARVESTER\|AIR SEEDER\|DISC SEEDER\|SPRAY RIG\|GRAIN AUGER' src/__tests__/describe-route.test.ts returns 5 or more matches
    - grep 'FORKLIFT (CLEARVIEW MAST\|WALKIE STACKER\|ELECTRIC PALLET JACK\|STOCK PICKER' src/__tests__/describe-route.test.ts returns 4 or more matches
    - grep 'CAMPER TRAILER\|MOTORHOME' src/__tests__/describe-route.test.ts returns 2 or more matches
    - grep 'TIMBER JINKER\|SKEL TRAILER\|WALKING FLOOR\|DOG / PIG / TAG' src/__tests__/describe-route.test.ts returns 4 matches
    - npm run test -- src/__tests__/describe-route.test.ts exits with failures (not 0) — confirming RED state
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run: `npm run test -- --reporter=verbose src/__tests__/describe-route.test.ts`
Expected: Many failures (all new DESCR-01..DESCR-08 tests RED), existing tests green or intentionally updated RED.
</verification>

<success_criteria>
Test scaffold complete with all DESCR requirements having automated test coverage. Every test that covers a new or changed template section is RED against the current prompt. Plans 02-04 will make them GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/17-description-template-coverage/17-01-SUMMARY.md`
</output>
