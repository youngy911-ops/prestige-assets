---
phase: 26-extraction-accuracy-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/schema-registry/schemas/marine.ts
  - src/__tests__/extraction-schema.test.ts
autonomous: true
requirements:
  - EXTRACT-03

must_haves:
  truths:
    - "Marine assets have a dedicated fuel_type field that AI can extract (Petrol / Diesel / Electric)"
    - "main_engine_details aiHint explicitly tells AI to separately list make, model, and HP as a combined string — no ambiguity about what format to use"
    - "Caravan trailer_length aiHint explicitly states value must be in FEET and gives explicit examples so AI does not accidentally output metres"
    - "All 404 existing tests still pass after schema changes"
  artifacts:
    - path: "src/lib/schema-registry/schemas/marine.ts"
      provides: "fuel_type field (aiExtractable: true) and improved main_engine_details aiHint"
    - path: "src/__tests__/extraction-schema.test.ts"
      provides: "marine field count updated from 18 to 19"
  key_links:
    - from: "src/lib/schema-registry/schemas/marine.ts"
      to: "src/__tests__/extraction-schema.test.ts"
      via: "getAIExtractableFields('marine').length assertion"
      pattern: "expect\\(aiFields\\.length\\)\\.toBe\\(19\\)"
---

<objective>
Fix three targeted gaps in marine and caravan schemas identified during the Phase 26 accuracy audit.

Purpose: Marine assets have no fuel_type field (significant omission — boats run on Petrol, Diesel, or Electric), the main_engine_details hint is ambiguous about expected format, and the caravan trailer_length hint is unclear that the value must be in feet. These gaps cause the AI to either skip fuel type entirely or produce inconsistently formatted engine and length fields.

Output: Updated marine.ts schema (fuel_type added, main_engine_details hint sharpened), updated caravan.ts schema (trailer_length hint strengthened), test count updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Current marine field count and test assertion to update -->
<!-- From src/__tests__/extraction-schema.test.ts line 237: -->
<!-- expect(aiFields.length).toBe(18) -->
<!-- After adding fuel_type: must change to toBe(19) -->

<!-- Current marine fields (aiExtractable: true) — 18 fields: -->
<!-- hin, make, model, year, builder, hull_material, motor_type, number_of_engines, -->
<!-- main_engine_details, engine_hours, steering_type, beam, loa, trailer_length, -->
<!-- winch, thrusters, damage, extras -->
<!-- Adding fuel_type as sfOrder 12.5 (insert between engine_hours sfOrder:11 and fuel_tank_capacity sfOrder:12) -->
<!-- New total: 19 aiExtractable fields -->

<!-- Current caravan trailer_length field: -->
<!-- { key: 'trailer_length', label: 'Length (ft)', sfOrder: 15, inputType: 'text', aiExtractable: true, -->
<!--   inspectionPriority: true, required: false, -->
<!--   aiHint: 'Length of caravan body in feet (Australian standard). Use build plate if visible, or estimate from photos. Common sizes: 16ft, 18ft, 19ft, 21ft, 22ft.' } -->
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add marine fuel_type field and sharpen main_engine_details hint</name>
  <files>src/lib/schema-registry/schemas/marine.ts</files>
  <action>
Make two targeted edits to the marine schema:

**Edit 1 — Add fuel_type field (insert between engine_hours and fuel_tank_capacity):**
After the `engine_hours` field definition (sfOrder: 11) and before `fuel_tank_capacity` (sfOrder: 12), insert a new field:

```typescript
{
  key: 'fuel_type', label: 'Fuel Type', sfOrder: 12, inputType: 'select',
  options: ['Petrol', 'Diesel', 'Electric'],
  aiExtractable: true,
  aiHint: 'Infer from engine type: outboard engines are almost always Petrol (2-stroke or 4-stroke); inboard/sterndrive diesel engines (Yanmar, Volvo Penta diesel, Cummins, Mercury Diesel) = Diesel; electric outboards (Torqeedo, ePropulsion) or vessels with no combustion engine = Electric. When in doubt for outboard vessels, default Petrol. When in doubt for cruiser/cabin boat with inboard engine, default Diesel.',
  required: false,
},
```

Update the existing `fuel_tank_capacity` sfOrder from 12 → 13, `water_tank_capacity` from 13 → 14, and all subsequent sfOrders in the marine schema upward by 1 (steering_type: 14→15, beam: 15→16, draft: 16→17, loa: 17→18, trailer_length: 18→19, launch_date: 19→20, sighted: 20→21, winch: 21→22, thrusters: 22→23, damage: 23→24, damage_notes: 24→25, extras: 25→26).

**Edit 2 — Sharpen main_engine_details aiHint:**
Replace the current aiHint for `main_engine_details` with a version that explicitly states the expected output format to remove ambiguity:

Append to the existing hint (after the last sentence about common HP ranges): `Output format: "[Make] [Model] [HP]hp" — e.g. "Yamaha F150 150hp", "Mercury 90hp FourStroke 90hp", "Volvo Penta D4-300 300hp". If HP is not determinable from the cowling badge, omit it.`

Do NOT rewrite the whole hint — only append this format guidance sentence.
  </action>
  <verify>
    <automated>cd /home/jack/projects/prestige_assets && npx vitest run src/__tests__/extraction-schema.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
- fuel_type field exists in marine schema between engine_hours and fuel_tank_capacity
- fuel_type has aiExtractable: true, options: ['Petrol', 'Diesel', 'Electric'], and the specified aiHint
- All sfOrders above engine_hours incremented correctly (no duplicates)
- main_engine_details aiHint has output format guidance appended
- The marine extraction schema test FAILS on field count (expected — Task 2 will fix it)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update marine test field count + strengthen caravan trailer_length hint</name>
  <files>
    src/__tests__/extraction-schema.test.ts
    src/lib/schema-registry/schemas/caravan.ts
  </files>
  <action>
**Edit 1 — Update marine test:**
In `src/__tests__/extraction-schema.test.ts`, find the line:
```
// marine has 18 aiExtractable fields
expect(aiFields.length).toBe(18)
```
Change to:
```
// marine has 19 aiExtractable fields (fuel_type added in Phase 26)
expect(aiFields.length).toBe(19)
```

**Edit 2 — Caravan trailer_length aiHint:**
In `src/lib/schema-registry/schemas/caravan.ts`, replace the existing aiHint for `trailer_length`:

Current: `'Length of caravan body in feet (Australian standard). Use build plate if visible, or estimate from photos. Common sizes: 16ft, 18ft, 19ft, 21ft, 22ft.'`

Replace with: `'ALWAYS output in FEET — never in metres. Australian caravans are sized in feet. Build plate may show "length" in mm or metres — convert to feet for output. Common caravan lengths: 16ft (≈4.9m), 17ft (≈5.2m), 18ft (≈5.5m), 19ft (≈5.8m), 20ft (≈6.1m), 21ft (≈6.4m), 22ft (≈6.7m), 23ft (≈7.0m). Motorhome body lengths typically 20–24ft. Output as a number followed by "ft" (e.g. "18ft", "21ft") — never "6.4m".'`
  </action>
  <verify>
    <automated>cd /home/jack/projects/prestige_assets && npx vitest run src/__tests__/extraction-schema.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
- Marine test expects 19 fields and passes
- All 404 tests pass (run full suite to confirm no regressions): `npx vitest run 2>&1 | tail -5`
- Caravan trailer_length aiHint explicitly says "ALWAYS output in FEET — never in metres"
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema → AI prompt | aiHint content is injected verbatim into the GPT-4o system prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | marine.ts sfOrder numbering | mitigate | After edit, verify no two fields share the same sfOrder with a quick grep |
| T-26-02 | Information Disclosure | fuel_type options | accept | Options are public boat knowledge; no PII involved |
</threat_model>

<verification>
1. `npx vitest run` — all 404+ tests pass (the +1 or +2 from new count assertion)
2. Manual spot-check: `grep -n "sfOrder" src/lib/schema-registry/schemas/marine.ts` — no duplicate sfOrder values
3. `grep -n "fuel_type" src/lib/schema-registry/schemas/marine.ts` — confirms field exists with aiExtractable: true
4. `grep "toBe(19)" src/__tests__/extraction-schema.test.ts` — confirms count updated
</verification>

<success_criteria>
- Marine schema has fuel_type field (aiExtractable: true, options Petrol/Diesel/Electric)
- Marine test passes with field count 19
- Caravan trailer_length aiHint says "ALWAYS output in FEET — never in metres"
- Full test suite green (404+ tests)
</success_criteria>

<output>
After completion, create `.planning/phases/26-extraction-accuracy-audit/26-01-SUMMARY.md`
</output>
