---
phase: 20-brand-config-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/constants/brand.ts
  - src/__tests__/brand.test.ts
  - src/app/layout.tsx
  - src/app/(auth)/login/page.tsx
  - src/components/asset/ReportClient.tsx
  - src/app/(app)/assets/[id]/output/page.tsx
autonomous: true
requirements: [BRAND-01, BRAND-02]

must_haves:
  truths:
    - "QR codes on output and report pages use a domain value sourced from brand config, not a hardcoded string"
    - "Company name, logo monogram, page title, and report header/footer all come from a single brand config import"
    - "The AI description prompt in /api/describe/route.ts is NOT modified"
  artifacts:
    - path: "src/lib/constants/brand.ts"
      provides: "Single source of truth for all brand strings and QR domain"
      exports: ["BRAND"]
      contains: "as const"
    - path: "src/__tests__/brand.test.ts"
      provides: "Unit tests for brand config exports and grep-based smoke test for hardcoded hex"
      contains: "BRAND.domain"
    - path: "src/app/layout.tsx"
      provides: "Root layout with metadata sourced from BRAND config"
      contains: "BRAND.appTitle"
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Login page using BRAND.name and BRAND.logoMonogram"
      contains: "BRAND.name"
    - path: "src/components/asset/ReportClient.tsx"
      provides: "Report with brand strings and QR domain from config"
      contains: "BRAND.domain"
    - path: "src/app/(app)/assets/[id]/output/page.tsx"
      provides: "Output page with QR domain from config"
      contains: "BRAND.domain"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/lib/constants/brand.ts"
      via: "import { BRAND }"
      pattern: "import.*BRAND.*from.*brand"
    - from: "src/components/asset/ReportClient.tsx"
      to: "src/lib/constants/brand.ts"
      via: "import { BRAND }"
      pattern: "BRAND\\.domain"
    - from: "src/app/(app)/assets/[id]/output/page.tsx"
      to: "src/lib/constants/brand.ts"
      via: "import { BRAND }"
      pattern: "BRAND\\.domain"
---

<objective>
Create a brand config module and wire all brand-visible strings (company name, logo monogram, page metadata, report text, QR domain) to it. After this plan, every brand string in the app is sourced from a single file.

Purpose: BRAND-01 (configurable QR domain) and BRAND-02 (single brand config for all UI strings) — eliminates scattered hardcoded brand values.
Output: `src/lib/constants/brand.ts` with all consumers importing from it; `src/__tests__/brand.test.ts` with passing tests.
</objective>

<execution_context>
@/home/jack/.claude/get-shit-done/workflows/execute-plan.md
@/home/jack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-brand-config-consolidation/20-CONTEXT.md
@.planning/phases/20-brand-config-consolidation/20-RESEARCH.md

<interfaces>
<!-- Existing constants pattern from src/lib/constants/branches.ts -->
```typescript
export const BRANCHES = [
  { key: 'brisbane', label: 'Brisbane (QLD)' },
  // ...
] as const
export type BranchKey = (typeof BRANCHES)[number]['key']
```

<!-- Current brand strings in files (to be replaced): -->
<!-- src/app/layout.tsx line 14: title: 'Slattery Auctions Book-in' -->
<!-- src/app/(auth)/login/page.tsx line 9: <span ...>S</span> -->
<!-- src/app/(auth)/login/page.tsx line 12: Slattery Auctions -->
<!-- src/components/asset/ReportClient.tsx line 56: Slattery Auctions — Condition Report -->
<!-- src/components/asset/ReportClient.tsx line 113: Generated by Slattery Auctions Asset Book-In · assetbookintool.com -->
<!-- src/app/(app)/assets/[id]/output/page.tsx line 84: assetbookintool.com in QR URL -->
<!-- src/components/asset/ReportClient.tsx line 60: assetbookintool.com in QR URL -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create brand config module and tests</name>
  <files>src/lib/constants/brand.ts, src/__tests__/brand.test.ts</files>
  <read_first>
    src/lib/constants/branches.ts
    src/app/globals.css
    src/app/layout.tsx
    src/app/(auth)/login/page.tsx
    src/components/asset/ReportClient.tsx
    src/app/(app)/assets/[id]/output/page.tsx
  </read_first>
  <behavior>
    - BRAND.name equals 'Slattery Auctions'
    - BRAND.logoMonogram equals 'S'
    - BRAND.appTitle equals 'Slattery Auctions Book-in'
    - BRAND.appDescription equals 'Asset book-in tool'
    - BRAND.reportHeader equals 'Slattery Auctions — Condition Report' (em dash, not hyphen)
    - BRAND.reportFooter equals 'Generated by Slattery Auctions Asset Book-In'
    - BRAND.domain equals 'assetbookintool.com'
    - All BRAND values are non-empty strings
    - Smoke test: grep src/components and src/app for hardcoded 'assetbookintool.com' string literals — should find zero matches after Plan 01 completes (this test will initially fail RED, which is expected)
  </behavior>
  <action>
Create `src/lib/constants/brand.ts` with:

```typescript
export const BRAND = {
  /** Visible company name — used in UI headings, report headers, page titles */
  name: 'Slattery Auctions',
  /** Short brand identifier for logo monogram */
  logoMonogram: 'S',
  /** Full app title for browser tab / metadata */
  appTitle: 'Slattery Auctions Book-in',
  /** Page metadata description */
  appDescription: 'Asset book-in tool',
  /** Report header label */
  reportHeader: 'Slattery Auctions \u2014 Condition Report',
  /** Report footer text */
  reportFooter: 'Generated by Slattery Auctions Asset Book-In',
  /** Domain used in QR codes — change this one value to update all QR codes */
  domain: 'assetbookintool.com',
} as const
```

Create `src/__tests__/brand.test.ts` with:
- Unit tests asserting each BRAND field is a non-empty string with the exact expected value
- Smoke test using `fs.readFileSync` + regex to scan `src/components/` and `src/app/` directories (recursively) for literal `assetbookintool.com` strings in `.tsx`/`.ts` files — this test asserts zero matches. Mark this test with `.todo` or `.skip` initially since the consumers haven't been updated yet; it will be unskipped in Task 2.
- Second smoke test: grep for hardcoded hex values `#F87171` in component files — also `.skip` initially (unskipped in Plan 02).

Follow the existing test patterns in `src/__tests__/` directory.
  </action>
  <verify>
    <automated>cd /home/jack/projects/prestige_assets && npx vitest run src/__tests__/brand.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/constants/brand.ts exists and contains `export const BRAND`
    - src/lib/constants/brand.ts contains `as const`
    - src/lib/constants/brand.ts contains `domain: 'assetbookintool.com'`
    - src/lib/constants/brand.ts contains `name: 'Slattery Auctions'`
    - src/lib/constants/brand.ts contains `logoMonogram: 'S'`
    - src/lib/constants/brand.ts contains `reportHeader: 'Slattery Auctions`
    - src/__tests__/brand.test.ts exists and contains `BRAND.domain`
    - src/__tests__/brand.test.ts exists and contains `BRAND.name`
    - `npx vitest run src/__tests__/brand.test.ts` exits 0
  </acceptance_criteria>
  <done>Brand config module exports all required fields with correct values; unit tests pass; smoke tests are defined but skipped until consumers are updated.</done>
</task>

<task type="auto">
  <name>Task 2: Wire brand config into all consumers</name>
  <files>src/app/layout.tsx, src/app/(auth)/login/page.tsx, src/components/asset/ReportClient.tsx, src/app/(app)/assets/[id]/output/page.tsx, src/__tests__/brand.test.ts</files>
  <read_first>
    src/lib/constants/brand.ts
    src/app/layout.tsx
    src/app/(auth)/login/page.tsx
    src/components/asset/ReportClient.tsx
    src/app/(app)/assets/[id]/output/page.tsx
    src/__tests__/brand.test.ts
    src/app/api/describe/route.ts
  </read_first>
  <action>
Wire `BRAND` imports into all four consumer files. DO NOT touch `src/app/api/describe/route.ts` — the AI prompt is locked.

**src/app/layout.tsx:**
- Add `import { BRAND } from '@/lib/constants/brand'`
- Change `title: 'Slattery Auctions Book-in'` to `title: BRAND.appTitle`
- Change `description: 'Asset book-in tool'` to `description: BRAND.appDescription`

**src/app/(auth)/login/page.tsx:**
- Add `import { BRAND } from '@/lib/constants/brand'`
- Change the hardcoded `S` in `<span className="text-2xl font-black text-white tracking-tight">S</span>` to `{BRAND.logoMonogram}`
- Change the hardcoded `Slattery Auctions` in the `<h1>` to `{BRAND.name}`

**src/components/asset/ReportClient.tsx:**
- Add `import { BRAND } from '@/lib/constants/brand'`
- Replace `Slattery Auctions — Condition Report` (line 56) with `{BRAND.reportHeader}`
- Replace `Generated by Slattery Auctions Asset Book-In · assetbookintool.com` (line 113) with `{BRAND.reportFooter} · {BRAND.domain}`
- Replace `assetbookintool.com` in the QR code URL (line 60) with `${BRAND.domain}`

**src/app/(app)/assets/[id]/output/page.tsx:**
- Add `import { BRAND } from '@/lib/constants/brand'`
- Replace `assetbookintool.com` in the QR code URL (line 84) with `${BRAND.domain}`

**src/__tests__/brand.test.ts:**
- Unskip the smoke test that greps for `assetbookintool.com` in source files — it should now pass (zero matches in consumer files; note: the brand.ts definition file itself contains the string as a value, so the grep must exclude `brand.ts` from its scan).

After all changes, verify that `src/app/api/describe/route.ts` still contains its original `Slattery Auctions` string — confirm it was NOT modified.
  </action>
  <verify>
    <automated>cd /home/jack/projects/prestige_assets && npx vitest run src/__tests__/brand.test.ts && grep -r "assetbookintool\.com" src/app src/components --include="*.tsx" --include="*.ts" -l | grep -v brand.ts | grep -c . | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
    - src/app/layout.tsx contains `import { BRAND } from '@/lib/constants/brand'`
    - src/app/layout.tsx contains `title: BRAND.appTitle`
    - src/app/layout.tsx does NOT contain the literal string `'Slattery Auctions Book-in'`
    - src/app/(auth)/login/page.tsx contains `import { BRAND } from '@/lib/constants/brand'`
    - src/app/(auth)/login/page.tsx contains `{BRAND.name}`
    - src/app/(auth)/login/page.tsx contains `{BRAND.logoMonogram}`
    - src/app/(auth)/login/page.tsx does NOT contain hardcoded `>S<` or `Slattery Auctions` as literal text
    - src/components/asset/ReportClient.tsx contains `import { BRAND } from '@/lib/constants/brand'`
    - src/components/asset/ReportClient.tsx contains `BRAND.domain`
    - src/components/asset/ReportClient.tsx contains `BRAND.reportHeader`
    - src/components/asset/ReportClient.tsx does NOT contain literal `assetbookintool.com`
    - src/app/(app)/assets/[id]/output/page.tsx contains `BRAND.domain`
    - src/app/(app)/assets/[id]/output/page.tsx does NOT contain literal `assetbookintool.com`
    - src/app/api/describe/route.ts still contains `Slattery Auctions` (NOT modified)
    - `npx vitest run src/__tests__/brand.test.ts` exits 0
  </acceptance_criteria>
  <done>All four consumer files import from BRAND config. No hardcoded brand strings remain outside of brand.ts and the locked AI prompt. QR domain is configurable from one place. Smoke tests pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/__tests__/brand.test.ts` — all unit and smoke tests pass
- `grep -r "assetbookintool\.com" src/ --include="*.tsx" --include="*.ts" -l` returns only `src/lib/constants/brand.ts`
- `grep -rn "Slattery Auctions" src/ --include="*.tsx" --include="*.ts"` returns only `src/lib/constants/brand.ts`, `src/app/api/describe/route.ts`, and `src/app/globals.css` (comment)
- `npm test` — full test suite passes
</verification>

<success_criteria>
- Brand config module exists at src/lib/constants/brand.ts with all 7 fields
- All 4 consumer files import BRAND and use its values instead of hardcoded strings
- QR domain appears in exactly 1 source-of-truth location (brand.ts)
- AI description prompt is untouched
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/20-brand-config-consolidation/20-01-SUMMARY.md`
</output>
