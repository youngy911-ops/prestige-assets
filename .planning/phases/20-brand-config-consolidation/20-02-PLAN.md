---
phase: 20-brand-config-consolidation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/asset/PhotoThumbnailGrid.tsx
  - src/components/auth/LoginForm.tsx
  - src/components/asset/UploadProgressIndicator.tsx
  - src/components/asset/PhotoThumbnail.tsx
  - src/components/asset/PhotoUploadZone.tsx
  - src/components/asset/OutputPanel.tsx
  - src/components/asset/AssetList.tsx
  - src/app/(app)/layout.tsx
  - src/app/(app)/assets/new/page.tsx
  - src/app/global-error.tsx
autonomous: true
requirements: [BRAND-03]

must_haves:
  truths:
    - "No component file contains hardcoded #F87171 — all use text-destructive, bg-destructive, or hover:text-destructive"
    - "The app layout uses a semantic Tailwind class instead of bg-[#166534]"
    - "AssetList dropdown uses bg-card instead of bg-[#111f11]"
    - "OutputPanel uses ring-offset-background instead of ring-offset-[#0a1a0a]"
    - "global-error.tsx inline hex values are updated to match brand palette"
  artifacts:
    - path: "src/components/auth/LoginForm.tsx"
      provides: "Login form with text-destructive for errors"
      contains: "text-destructive"
    - path: "src/components/asset/PhotoThumbnailGrid.tsx"
      provides: "Photo grid with text-destructive for errors"
      contains: "text-destructive"
    - path: "src/app/(app)/layout.tsx"
      provides: "App layout with semantic background class"
    - path: "src/app/global-error.tsx"
      provides: "Global error boundary with brand-matching hex values"
  key_links:
    - from: "src/components/*/**.tsx"
      to: "src/app/globals.css"
      via: "Tailwind semantic classes (text-destructive, bg-card, etc.)"
      pattern: "text-destructive|bg-destructive|bg-card|ring-offset-background"
---

<objective>
Replace all hardcoded hex color values in component files with semantic Tailwind tokens that are already defined in the CSS custom properties system.

Purpose: BRAND-03 — eliminate scattered hardcoded colors so the entire color palette is controlled via CSS variables in globals.css.
Output: 10 files updated; zero hardcoded hex color values remain in component files.
</objective>

<execution_context>
@/home/jack/.claude/get-shit-done/workflows/execute-plan.md
@/home/jack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-brand-config-consolidation/20-CONTEXT.md
@.planning/phases/20-brand-config-consolidation/20-RESEARCH.md

<interfaces>
<!-- From src/app/globals.css — existing semantic CSS custom properties: -->
```css
--destructive: oklch(0.74 0.15 20);    /* equivalent to #F87171 red */
--background:  oklch(0.16 0.04 148);   /* deep dark green, hex approx #0f1f0f */
--card:        oklch(0.20 0.05 148);   /* slightly elevated dark green */
--primary:     oklch(0.52 0.17 148);   /* bright green CTA */
```

<!-- @theme inline bridges: -->
```css
--color-destructive: var(--destructive);  /* enables: text-destructive, bg-destructive */
--color-background:  var(--background);   /* enables: bg-background */
--color-card:        var(--card);         /* enables: bg-card */
```

<!-- Substitution map (from RESEARCH.md): -->
<!-- text-[#F87171]      → text-destructive       (6 occurrences across 6 files) -->
<!-- bg-[#F87171]/60     → bg-destructive/60      (UploadProgressIndicator.tsx) -->
<!-- hover:text-[#F87171]→ hover:text-destructive  (PhotoThumbnail.tsx) -->
<!-- bg-[#166534]        → bg-background           (app/layout.tsx — verify visually) -->
<!-- bg-[#111f11]        → bg-card                 (AssetList.tsx dropdown) -->
<!-- ring-offset-[#0a1a0a]→ ring-offset-background (OutputPanel.tsx) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded hex colors with semantic Tailwind tokens</name>
  <files>src/components/asset/PhotoThumbnailGrid.tsx, src/components/auth/LoginForm.tsx, src/components/asset/UploadProgressIndicator.tsx, src/components/asset/PhotoThumbnail.tsx, src/components/asset/PhotoUploadZone.tsx, src/components/asset/OutputPanel.tsx, src/components/asset/AssetList.tsx, src/app/(app)/layout.tsx, src/app/(app)/assets/new/page.tsx, src/app/global-error.tsx</files>
  <read_first>
    src/app/globals.css
    src/components/asset/PhotoThumbnailGrid.tsx
    src/components/auth/LoginForm.tsx
    src/components/asset/UploadProgressIndicator.tsx
    src/components/asset/PhotoThumbnail.tsx
    src/components/asset/PhotoUploadZone.tsx
    src/components/asset/OutputPanel.tsx
    src/components/asset/AssetList.tsx
    src/app/(app)/layout.tsx
    src/app/(app)/assets/new/page.tsx
    src/app/global-error.tsx
  </read_first>
  <action>
Apply the following exact substitutions in each file. Each change is a single class name replacement — no structural changes to the components.

**1. src/components/asset/PhotoThumbnailGrid.tsx (line 144):**
Change: `text-[#F87171]`
To: `text-destructive`

**2. src/components/auth/LoginForm.tsx (line 59):**
Change: `text-[#F87171]`
To: `text-destructive`

**3. src/components/asset/UploadProgressIndicator.tsx (line 15):**
Change: `bg-[#F87171]/60`
To: `bg-destructive/60`

**4. src/components/asset/PhotoThumbnail.tsx (line 57):**
Change: `hover:text-[#F87171]`
To: `hover:text-destructive`

**5. src/components/asset/PhotoUploadZone.tsx (lines 226 and 322):**
Change both: `text-[#F87171]`
To: `text-destructive`

**6. src/app/(app)/assets/new/page.tsx (line 136):**
Change: `text-[#F87171]`
To: `text-destructive`

**7. src/components/asset/OutputPanel.tsx (line 144):**
Change: `ring-offset-[#0a1a0a]`
To: `ring-offset-background`

**8. src/components/asset/AssetList.tsx (line 106):**
Change: `bg-[#111f11]`
To: `bg-card`

**9. src/app/(app)/layout.tsx (line 10):**
Change: `bg-[#166534]`
To: `bg-background`
NOTE: The body already has `background-color` set via `--background` in globals.css. The layout wrapper div's `bg-[#166534]` (mid-green, oklch ~0.39) is visually distinct from `--background` (near-black, oklch 0.16). However, the body background already fills the viewport. Replace with `bg-background` for consistency — the body color dominates. If this causes a visible issue, the checkpoint in verification will catch it.

**10. src/app/global-error.tsx (line 12):**
This file uses inline `style={{}}` because it renders before CSS loads. Update the hex values to match the brand palette:
- Change `background: '#166534'` to `background: '#0f1f0f'` (matches --background oklch(0.16 0.04 148))
- If there is a button with `background: '#059669'`, change to `background: '#3a7a3a'` (matches --primary oklch(0.52 0.17 148) approximately)
Keep inline styles — do NOT convert to Tailwind classes in this file (error boundary cannot rely on stylesheet loading).

**IMPORTANT:** Do NOT modify `LAST_BRANCH_KEY` or any other non-color code in these files. Phase 23 (CODE-01) handles constant consolidation separately. Only change color-related class names and inline style hex values.
  </action>
  <verify>
    <automated>cd /home/jack/projects/prestige_assets && grep -rn "#F87171\|#166534\|#111f11\|#0a1a0a" src/components/ src/app/(app)/ src/app/\(auth\)/ --include="*.tsx" --include="*.ts" | grep -v node_modules | grep -v ".test." && echo "FAIL: hardcoded hex found" && exit 1 || echo "PASS: no hardcoded hex in components"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/asset/PhotoThumbnailGrid.tsx contains `text-destructive` and does NOT contain `#F87171`
    - src/components/auth/LoginForm.tsx contains `text-destructive` and does NOT contain `#F87171`
    - src/components/asset/UploadProgressIndicator.tsx contains `bg-destructive/60` and does NOT contain `#F87171`
    - src/components/asset/PhotoThumbnail.tsx contains `hover:text-destructive` and does NOT contain `#F87171`
    - src/components/asset/PhotoUploadZone.tsx contains `text-destructive` (2 occurrences) and does NOT contain `#F87171`
    - src/app/(app)/assets/new/page.tsx contains `text-destructive` and does NOT contain `#F87171`
    - src/components/asset/OutputPanel.tsx contains `ring-offset-background` and does NOT contain `#0a1a0a`
    - src/components/asset/AssetList.tsx contains `bg-card` and does NOT contain `#111f11`
    - src/app/(app)/layout.tsx contains `bg-background` and does NOT contain `#166534`
    - src/app/global-error.tsx contains `background: '#0f1f0f'` and does NOT contain `#166534`
    - `npm test` exits 0 (full suite passes)
  </acceptance_criteria>
  <done>All 10 files updated. Zero hardcoded hex color values (#F87171, #166534, #111f11, #0a1a0a) remain in component files. All colors now use semantic Tailwind tokens backed by CSS custom properties in globals.css, or brand-matching hex in the global error boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Enable hex smoke test and run full validation</name>
  <files>src/__tests__/brand.test.ts</files>
  <read_first>
    src/__tests__/brand.test.ts
  </read_first>
  <action>
In `src/__tests__/brand.test.ts` (created by Plan 01), unskip the smoke test that greps component files for hardcoded `#F87171` hex values. The test should:

1. Use `fs` and `path` to recursively read all `.tsx` and `.ts` files under `src/components/` and `src/app/` (excluding `node_modules`, `.test.` files, `globals.css`, and `brand.ts`)
2. Assert that NONE of them contain the strings `#F87171`, `#166534`, `#111f11`, or `#0a1a0a`
3. Allow `global-error.tsx` to contain `#0f1f0f` (the updated brand-matching dark background hex) — this is intentional

If Plan 01 has not yet run (the test file doesn't exist), create it with all tests including the hex smoke test enabled from the start.

Run the full test suite to confirm everything passes.
  </action>
  <verify>
    <automated>cd /home/jack/projects/prestige_assets && npx vitest run src/__tests__/brand.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - src/__tests__/brand.test.ts does NOT contain `.skip` or `.todo` for the hex smoke test
    - src/__tests__/brand.test.ts contains `#F87171` as a string being searched for (in the grep test)
    - `npx vitest run src/__tests__/brand.test.ts` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
  <done>All smoke tests enabled and passing. Full test suite green. BRAND-03 is fully verified — no hardcoded hex colors remain in component files.</done>
</task>

</tasks>

<verification>
- `grep -rn "#F87171\|#166534\|#111f11\|#0a1a0a" src/components/ src/app/ --include="*.tsx" --include="*.ts" | grep -v globals.css | grep -v brand.test | grep -v global-error` returns zero matches
- `npx vitest run src/__tests__/brand.test.ts` — all tests pass including hex smoke test
- `npm test` — full test suite passes
- Visual spot-check: `src/app/global-error.tsx` uses `#0f1f0f` (dark green matching --background) instead of old `#166534`
</verification>

<success_criteria>
- All 10 files have hardcoded hex replaced with semantic Tailwind tokens or brand-matching hex (global-error only)
- Hex smoke test in brand.test.ts is enabled and passing
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/20-brand-config-consolidation/20-02-SUMMARY.md`
</output>
