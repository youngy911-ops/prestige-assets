---
phase: 18-test-key-fidelity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/describe-route.test.ts
autonomous: true
requirements:
  - DESCR-04
  - DESCR-06
  - DESCR-08

must_haves:
  truths:
    - "Line 1034 passes 'washing' (not 'washing_plant') as subtype key"
    - "Line 1134 passes 'stock_picker' (not 'order_picker') as subtype key"
    - "Line 1139 passes 'ewp' (not 'ewp_forklift') as subtype key"
    - "Line 1179 passes 'private' (not 'boat') as subtype key"
    - "Line 1194 passes 'commercial' (not 'commercial_vessel') as subtype key"
    - "Line 1204 passes 'tug' (not 'tug_workboat') as subtype key"
    - "All 105 tests in the full suite remain green after the corrections"
  artifacts:
    - path: "src/__tests__/describe-route.test.ts"
      provides: "Corrected phantom-key tests"
      contains: "getSystemContentP17('earthmoving', 'washing')"
  key_links:
    - from: "describe-route.test.ts"
      to: "earthmoving.ts schema"
      via: "subtype key string argument"
      pattern: "getSystemContentP17\\('earthmoving', 'washing'\\)"
    - from: "describe-route.test.ts"
      to: "forklift.ts schema"
      via: "subtype key string argument"
      pattern: "getSystemContentP17\\('forklift', 'stock_picker'\\)"
    - from: "describe-route.test.ts"
      to: "marine.ts schema"
      via: "subtype key string argument"
      pattern: "getSystemContentP17\\('marine', 'private'\\)"
---

<objective>
Replace six phantom subtype key strings in describe-route.test.ts with the real schema keys they are supposed to exercise. No production files are touched.

Purpose: Five test locations (six string edits) currently pass phantom subtype keys — keys that do not exist in the schema — to getSystemContentP17(). Because the helper returns the full static DESCRIPTION_SYSTEM_PROMPT regardless of the subtype argument, every toContain() assertion is green even when the key is wrong. Correcting the keys eliminates false CI confidence and ensures the tests exercise the same code path a real user interaction would trigger.

Output: src/__tests__/describe-route.test.ts with 6 corrected subtype key strings; full 105-test suite green.
</objective>

<execution_context>
@/home/jack/.claude/get-shit-done/workflows/execute-plan.md
@/home/jack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-test-key-fidelity/18-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace six phantom subtype keys with real schema keys</name>
  <files>src/__tests__/describe-route.test.ts</files>

  <read_first>
    - src/__tests__/describe-route.test.ts (lines 1030–1210 — confirm exact current text before editing)
    - src/lib/schema-registry/schemas/earthmoving.ts (confirm `washing` key exists, not `washing_plant`)
    - src/lib/schema-registry/schemas/forklift.ts (confirm `stock_picker` and `ewp` keys exist)
    - src/lib/schema-registry/schemas/marine.ts (confirm `private`, `commercial`, `tug` keys exist)
  </read_first>

  <action>
Make exactly six string replacements in src/__tests__/describe-route.test.ts. Change ONLY the second argument to getSystemContentP17() at each location. Do NOT change any toContain() assertion string, any it() description text, or any other code.

Replacement 1 — line 1034 (earthmoving washing):
  FROM: const s = await getSystemContentP17('earthmoving', 'washing_plant')
  TO:   const s = await getSystemContentP17('earthmoving', 'washing')

Replacement 2 — line 1134 (forklift stock_picker):
  FROM: const s = await getSystemContentP17('forklift', 'order_picker')
  TO:   const s = await getSystemContentP17('forklift', 'stock_picker')

Replacement 3 — line 1139 (forklift ewp):
  FROM: const s = await getSystemContentP17('forklift', 'ewp_forklift')
  TO:   const s = await getSystemContentP17('forklift', 'ewp')

Replacement 4 — line 1179 (marine private):
  FROM: const s = await getSystemContentP17('marine', 'boat')
  TO:   const s = await getSystemContentP17('marine', 'private')

Replacement 5 — line 1194 (marine commercial):
  FROM: const s = await getSystemContentP17('marine', 'commercial_vessel')
  TO:   const s = await getSystemContentP17('marine', 'commercial')

Replacement 6 — line 1204 (marine tug):
  FROM: const s = await getSystemContentP17('marine', 'tug_workboat')
  TO:   const s = await getSystemContentP17('marine', 'tug')

IMPORTANT — what must NOT change:
  - toContain('WASHING PLANT') stays as-is (prompt heading, not the key)
  - toContain('STOCK PICKER / ORDER PICKER') stays as-is
  - toContain('EWP (FORKLIFT-MOUNTED)') stays as-is
  - toContain('MARINE (RECREATIONAL BOAT)') stays as-is (no PRIVATE heading exists yet — Phase 19 adds it)
  - toContain('COMMERCIAL VESSEL') stays as-is
  - toContain('TUG / WORKBOAT') stays as-is
  - No production files (route.ts, schema files, system prompt) are modified

Boundary: Do NOT add new describe blocks or new it() tests. Phase 18 corrects existing phantom keys only. Tests for 'recreational' and 'personal_watercraft' belong to Phase 19.
  </action>

  <verify>
    <automated>npx vitest run src/__tests__/describe-route.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - grep -n "washing_plant" src/__tests__/describe-route.test.ts returns no output (phantom removed)
    - grep -n "order_picker" src/__tests__/describe-route.test.ts returns no output (phantom removed)
    - grep -n "ewp_forklift" src/__tests__/describe-route.test.ts returns no output (phantom removed)
    - grep -n "'boat'" src/__tests__/describe-route.test.ts returns no output (phantom removed)
    - grep -n "commercial_vessel" src/__tests__/describe-route.test.ts returns no output (phantom removed)
    - grep -n "tug_workboat" src/__tests__/describe-route.test.ts returns no output (phantom removed)
    - grep -n "getSystemContentP17('earthmoving', 'washing')" src/__tests__/describe-route.test.ts outputs line 1034
    - grep -n "getSystemContentP17('forklift', 'stock_picker')" src/__tests__/describe-route.test.ts outputs a match
    - grep -n "getSystemContentP17('forklift', 'ewp')" src/__tests__/describe-route.test.ts outputs a match
    - grep -n "getSystemContentP17('marine', 'private')" src/__tests__/describe-route.test.ts outputs a match
    - grep -n "getSystemContentP17('marine', 'commercial')" src/__tests__/describe-route.test.ts outputs a match
    - grep -n "getSystemContentP17('marine', 'tug')" src/__tests__/describe-route.test.ts outputs a match
    - npx vitest run src/__tests__/describe-route.test.ts exits 0 with all tests passing
    - npx vitest run exits 0 (full suite green, 105 tests)
  </acceptance_criteria>

  <done>Six phantom subtype strings replaced with real schema keys; all 105 tests pass; no production files modified.</done>
</task>

</tasks>

<verification>
Full suite command: `npx vitest run`

Expected result: 105 tests pass, 0 failures.

Spot-check grep commands to confirm no phantom keys remain:
- `grep -n "washing_plant\|order_picker\|ewp_forklift\|'boat'\|commercial_vessel\|tug_workboat" src/__tests__/describe-route.test.ts` — must return no output
</verification>

<success_criteria>
1. `grep -n "washing_plant" src/__tests__/describe-route.test.ts` returns empty
2. `grep -n "order_picker" src/__tests__/describe-route.test.ts` returns empty
3. `grep -n "ewp_forklift" src/__tests__/describe-route.test.ts` returns empty
4. `grep -n "'boat'" src/__tests__/describe-route.test.ts` returns empty
5. `grep -n "commercial_vessel" src/__tests__/describe-route.test.ts` returns empty
6. `grep -n "tug_workboat" src/__tests__/describe-route.test.ts` returns empty
7. `npx vitest run` exits 0 with 105 tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/18-test-key-fidelity/18-01-SUMMARY.md`
</output>
