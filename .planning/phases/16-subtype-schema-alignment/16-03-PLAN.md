---
phase: 16-subtype-schema-alignment
plan: 03
type: execute
wave: 2
depends_on:
  - 16-01
  - 16-02
files_modified:
  - src/__tests__/schema-registry.test.ts
autonomous: true
requirements:
  - SUBTYPE-01
  - SUBTYPE-02
  - SUBTYPE-03
  - SUBTYPE-04
  - SUBTYPE-05
  - SUBTYPE-06
  - SUBTYPE-07
  - SUBTYPE-08

must_haves:
  truths:
    - "npm test passes with zero failures"
    - "Test suite asserts correct counts for all 8 asset types post-Phase-16 schemas"
    - "Tests verify key presence/absence that proves each SUBTYPE-0N requirement is met"
  artifacts:
    - path: "src/__tests__/schema-registry.test.ts"
      provides: "Updated test assertions for all 8 schema types"
      contains: "toHaveLength(24)"
  key_links:
    - from: "src/__tests__/schema-registry.test.ts"
      to: "src/lib/schema-registry/schemas/*.ts"
      via: "getSubtypes() calls verified against new arrays"
      pattern: "getSubtypes\\('truck'\\)"
---

<objective>
Update `schema-registry.test.ts` so all assertions match the new subtype arrays introduced in Plans 01 and 02. Then run the full test suite and confirm zero failures.

Purpose: Plans 01 and 02 replaced all 8 subtype arrays — the test file still has the old hard-coded counts and key assertions which now fail. This plan brings the tests in line with the new state of the world, confirming correctness via automated verification.
Output: Updated test file. Full suite green.
</objective>

<execution_context>
@/home/jack/.claude/get-shit-done/workflows/execute-plan.md
@/home/jack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-subtype-schema-alignment/16-CONTEXT.md
@.planning/phases/16-subtype-schema-alignment/16-RESEARCH.md

<interfaces>
<!-- Test framework: Vitest. Run command: npm test -->
<!-- Quick targeted run: npm test -- --reporter=verbose src/__tests__/schema-registry.test.ts -->
<!-- Full suite: npm test -->

<!-- New subtype counts (post-Plans 01 and 02): -->
<!--   truck: 24    trailer: 24    earthmoving: 19 -->
<!--   agriculture: 12   forklift: 9   caravan: 5 -->
<!--   marine: 10   general_goods: 16 -->

<!-- Key changes to test against: -->
<!--   truck: service_truck (was service), no tipper, coupe added -->
<!--   trailer: no extendable, no drop_deck, walking_floor added -->
<!--   earthmoving: bulldozer_crawler_tractor (merged), backhoe (was backhoe_loader), no telehandler, no trencher -->
<!--   agriculture: 12 new SF keys replacing 6 old keys -->
<!--   forklift: 9 new SF keys, telehandler retained, counterbalance/reach_truck/order_picker removed -->
<!--   caravan: motorhome (was motor_home), coupe added -->
<!--   marine: 10 new SF keys, boat/yacht/jet_ski removed -->
<!--   general_goods: 16 new SF keys replacing old 5 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite schema-registry.test.ts subtype assertions to match new arrays</name>
  <files>
    src/__tests__/schema-registry.test.ts
  </files>
  <read_first>
    - src/__tests__/schema-registry.test.ts — current test file with all failing assertions to understand full test structure
    - src/lib/schema-registry/schemas/truck.ts — verify 24 entries are in place (from Plan 01)
    - src/lib/schema-registry/schemas/earthmoving.ts — verify 19 entries with bulldozer_crawler_tractor (from Plan 01)
    - src/lib/schema-registry/schemas/general-goods.ts — verify 16 entries (from Plan 02)
    - .planning/phases/16-subtype-schema-alignment/16-RESEARCH.md — section "Test pattern that must be updated" for example updated assertions
  </read_first>
  <action>
Rewrite the subtype-specific test cases in `src/__tests__/schema-registry.test.ts`. The non-subtype tests (FieldDefinition completeness, AI-extractable fields, aiHint convention enforcement) must NOT change — they remain valid.

Replace the entire `SCHEMA_REGISTRY structure` describe block with the following updated assertions:

```typescript
describe('SCHEMA_REGISTRY structure', () => {
  it('contains exactly 8 asset types', () => {
    expect(Object.keys(SCHEMA_REGISTRY)).toHaveLength(8)
  })

  it('ASSET_TYPES tuple has exactly 8 entries', () => {
    expect(ASSET_TYPES).toHaveLength(8)
  })

  it('every asset type in ASSET_TYPES has a corresponding registry entry', () => {
    for (const type of ASSET_TYPES) {
      expect(SCHEMA_REGISTRY[type]).toBeDefined()
    }
  })

  it('every schema has at least 1 subtype', () => {
    for (const type of ASSET_TYPES) {
      const schema = getSchema(type)
      expect(schema.subtypes.length).toBeGreaterThanOrEqual(1)
    }
  })

  it('truck has exactly 24 subtypes — 21 SF plus EWP, Tilt Tray, Flat Deck', () => {
    const subtypes = getSubtypes('truck')
    expect(subtypes).toHaveLength(24)
    expect(subtypes.map(s => s.key)).toContain('service_truck')
    expect(subtypes.map(s => s.key)).not.toContain('service')
    expect(subtypes.map(s => s.key)).toContain('crane_truck')
    expect(subtypes.map(s => s.key)).toContain('coupe')
    expect(subtypes.map(s => s.key)).toContain('ewp')
    expect(subtypes.map(s => s.key)).toContain('tilt_tray')
    expect(subtypes.map(s => s.key)).toContain('flat_deck')
    expect(subtypes.map(s => s.key)).toContain('prime_mover')
    expect(subtypes.map(s => s.key)).not.toContain('tipper')
    expect(subtypes.map(s => s.key)).toContain('other')
    const concrete_agitator = subtypes.find(s => s.key === 'concrete_agitator')
    expect(concrete_agitator?.label).toBe('Concrete - Agitator')
    const concrete_pump = subtypes.find(s => s.key === 'concrete_pump')
    expect(concrete_pump?.label).toBe('Concrete - Pump')
  })

  it('trailer has exactly 24 subtypes matching SF list', () => {
    const subtypes = getSubtypes('trailer')
    expect(subtypes).toHaveLength(24)
    expect(subtypes.map(s => s.key)).toContain('walking_floor')
    expect(subtypes.map(s => s.key)).toContain('refrigerated_curtainsider')
    expect(subtypes.map(s => s.key)).toContain('timber_jinker')
    expect(subtypes.map(s => s.key)).toContain('low_loader')
    expect(subtypes.map(s => s.key)).toContain('coupe')
    expect(subtypes.map(s => s.key)).not.toContain('extendable')
    expect(subtypes.map(s => s.key)).not.toContain('drop_deck')
  })

  it('earthmoving has exactly 19 subtypes with Bulldozer/Crawler Tractor merged', () => {
    const subtypes = getSubtypes('earthmoving')
    expect(subtypes).toHaveLength(19)
    expect(subtypes.map(s => s.key)).toContain('bulldozer_crawler_tractor')
    expect(subtypes.map(s => s.key)).not.toContain('bulldozer')
    expect(subtypes.map(s => s.key)).not.toContain('crawler_tractor')
    expect(subtypes.map(s => s.key)).toContain('backhoe')
    expect(subtypes.map(s => s.key)).not.toContain('backhoe_loader')
    expect(subtypes.map(s => s.key)).not.toContain('telehandler')
    expect(subtypes.map(s => s.key)).not.toContain('trencher')
    expect(subtypes.map(s => s.key)).toContain('skid_steer_loader')
    expect(subtypes.map(s => s.key)).toContain('motor_grader')
    expect(subtypes.map(s => s.key)).toContain('other')
    const merged = subtypes.find(s => s.key === 'bulldozer_crawler_tractor')
    expect(merged?.label).toBe('Bulldozer/Crawler Tractor')
    const conveyors = subtypes.find(s => s.key === 'conveyors_stackers')
    expect(conveyors?.label).toBe('Conveyors / Stackers')
  })

  it('agriculture has exactly 12 subtypes — new SF selector', () => {
    const subtypes = getSubtypes('agriculture')
    expect(subtypes).toHaveLength(12)
    expect(subtypes.map(s => s.key)).toContain('mower_conditioner')
    expect(subtypes.map(s => s.key)).toContain('combine_harvester')
    expect(subtypes.map(s => s.key)).toContain('air_seeder')
    expect(subtypes.map(s => s.key)).toContain('coupe')
    expect(subtypes.map(s => s.key)).not.toContain('header')
    expect(subtypes.map(s => s.key)).not.toContain('sprayer')
    expect(subtypes.map(s => s.key)).not.toContain('planter')
    expect(subtypes.map(s => s.key)).not.toContain('cultivation')
    const mower = subtypes.find(s => s.key === 'mower_conditioner')
    expect(mower?.label).toBe('Mower/Conditioner')
  })

  it('forklift has exactly 9 subtypes — new SF selector with telehandler', () => {
    const subtypes = getSubtypes('forklift')
    expect(subtypes).toHaveLength(9)
    expect(subtypes.map(s => s.key)).toContain('telehandler')
    expect(subtypes.map(s => s.key)).toContain('electric_pallet_jack')
    expect(subtypes.map(s => s.key)).toContain('walkie_stacker')
    expect(subtypes.map(s => s.key)).toContain('ewp')
    expect(subtypes.map(s => s.key)).not.toContain('counterbalance')
    expect(subtypes.map(s => s.key)).not.toContain('reach_truck')
    expect(subtypes.map(s => s.key)).not.toContain('order_picker')
  })

  it('caravan has exactly 5 subtypes — motorhome key (no space)', () => {
    const subtypes = getSubtypes('caravan')
    expect(subtypes).toHaveLength(5)
    expect(subtypes.map(s => s.key)).toContain('motorhome')
    expect(subtypes.map(s => s.key)).not.toContain('motor_home')
    expect(subtypes.map(s => s.key)).toContain('coupe')
    expect(subtypes.map(s => s.key)).toContain('camper_trailer')
    const motorhome = subtypes.find(s => s.key === 'motorhome')
    expect(motorhome?.label).toBe('Motorhome')
  })

  it('marine has exactly 10 subtypes replacing Boat/Yacht/Jet Ski', () => {
    const subtypes = getSubtypes('marine')
    expect(subtypes).toHaveLength(10)
    expect(subtypes.map(s => s.key)).toContain('personal_watercraft')
    expect(subtypes.map(s => s.key)).toContain('trailer_boat')
    expect(subtypes.map(s => s.key)).toContain('tug')
    expect(subtypes.map(s => s.key)).toContain('coupe')
    expect(subtypes.map(s => s.key)).not.toContain('boat')
    expect(subtypes.map(s => s.key)).not.toContain('yacht')
    expect(subtypes.map(s => s.key)).not.toContain('jet_ski')
  })

  it('general_goods has exactly 16 subtypes matching SF list', () => {
    const subtypes = getSubtypes('general_goods')
    expect(subtypes).toHaveLength(16)
    expect(subtypes.map(s => s.key)).toContain('jewellery_watches_collectables')
    expect(subtypes.map(s => s.key)).toContain('gardening_landscaping')
    expect(subtypes.map(s => s.key)).toContain('tools_toolboxes')
    expect(subtypes.map(s => s.key)).toContain('plant_equipment')
    expect(subtypes.map(s => s.key)).not.toContain('tools_equipment')
    expect(subtypes.map(s => s.key)).not.toContain('workshop_equipment')
    expect(subtypes.map(s => s.key)).not.toContain('office_it')
    const jewellery = subtypes.find(s => s.key === 'jewellery_watches_collectables')
    expect(jewellery?.label).toBe('Jewellery/Watches/Collectables')
    const gardening = subtypes.find(s => s.key === 'gardening_landscaping')
    expect(gardening?.label).toBe('Gardening & Landscaping')
  })

  it('caravan has hasGlassValuation: true', () => {
    expect(getSchema('caravan').hasGlassValuation).toBe(true)
  })

  it('no other type has hasGlassValuation: true', () => {
    const typesWithGlass = ASSET_TYPES.filter(t => t !== 'caravan' && getSchema(t).hasGlassValuation)
    expect(typesWithGlass).toHaveLength(0)
  })
})
```

The three describe blocks below `SCHEMA_REGISTRY structure` — `FieldDefinition completeness`, `AI-extractable fields`, and `aiHint convention enforcement` — must remain exactly as they are. Do not modify them.

After updating the file, run the targeted test command to confirm green before marking done.
  </action>
  <verify>
    <automated>cd /home/jack/projects/prestige_assets && npm test -- --reporter=verbose src/__tests__/schema-registry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --reporter=verbose src/__tests__/schema-registry.test.ts` exits 0 with 0 failures
    - src/__tests__/schema-registry.test.ts contains `toHaveLength(24)` (truck and trailer count assertions)
    - src/__tests__/schema-registry.test.ts contains `toHaveLength(19)` (earthmoving count assertion)
    - src/__tests__/schema-registry.test.ts contains `toHaveLength(16)` (general_goods count assertion)
    - src/__tests__/schema-registry.test.ts contains `bulldozer_crawler_tractor` (merged key assertion)
    - src/__tests__/schema-registry.test.ts contains `service_truck` (renamed key assertion)
    - src/__tests__/schema-registry.test.ts does NOT contain `toHaveLength(15)` (old truck count)
    - src/__tests__/schema-registry.test.ts does NOT contain `toHaveLength(12)` (old earthmoving count)
    - src/__tests__/schema-registry.test.ts does NOT contain `toHaveLength(5)` referencing general_goods (old count)
    - src/__tests__/schema-registry.test.ts still contains the `FieldDefinition completeness` describe block (unchanged)
    - src/__tests__/schema-registry.test.ts still contains the `AI-extractable fields` describe block (unchanged)
    - src/__tests__/schema-registry.test.ts still contains the `aiHint convention enforcement` describe block (unchanged)
  </acceptance_criteria>
  <done>All subtype count and key assertions updated. Targeted test run exits 0 with zero failures.</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite and confirm green</name>
  <files></files>
  <read_first>
    - src/__tests__/schema-registry.test.ts — verify Task 1 changes are in place before running full suite
  </read_first>
  <action>
Run the full test suite to confirm no regressions beyond the schema-registry changes were introduced by Plans 01, 02, and 03.

```bash
cd /home/jack/projects/prestige_assets && npm test
```

If any tests fail:
- If the failing test is in `schema-registry.test.ts`, the assertion was missed in Task 1 — fix it and re-run
- If the failing test is in any other file, examine whether it references old subtype keys (`service`, `backhoe_loader`, `bulldozer`, `crawler_tractor`, `motor_home`, `jet_ski`, `tools_equipment`, etc.) — if so, update those assertions to use the new keys
- Do NOT change any production code (schema files) to fix test failures — only update test assertions

The full suite must exit 0 before this task is marked done.
  </action>
  <verify>
    <automated>cd /home/jack/projects/prestige_assets && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0
    - Output shows 0 failing tests
    - All tests in `src/__tests__/schema-registry.test.ts` pass
    - No test failures introduced in any other test file
  </acceptance_criteria>
  <done>Full test suite exits 0. Phase 16 implementation is verified complete via automated tests.</done>
</task>

</tasks>

<verification>
Final check after both tasks complete:

```bash
cd /home/jack/projects/prestige_assets && npm test
# Must exit 0 with 0 failures

grep "toHaveLength(24)" src/__tests__/schema-registry.test.ts
# Must match at least twice (truck, trailer)

grep "toHaveLength(19)" src/__tests__/schema-registry.test.ts
# Must match (earthmoving)

grep "bulldozer_crawler_tractor" src/__tests__/schema-registry.test.ts
# Must match (merged key assertion present)
```
</verification>

<success_criteria>
- `npm test` exits 0 with zero failures
- schema-registry.test.ts has correct count assertions for all 8 types (24, 24, 19, 12, 9, 5, 10, 16)
- schema-registry.test.ts has key presence/absence assertions proving each SUBTYPE requirement
- Non-subtype tests (FieldDefinition, AI-extractable, aiHint) remain unchanged and passing
</success_criteria>

<output>
After completion, create `.planning/phases/16-subtype-schema-alignment/16-03-SUMMARY.md`
</output>
