---
phase: 16-subtype-schema-alignment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/schema-registry/schemas/agriculture.ts
  - src/lib/schema-registry/schemas/forklift.ts
  - src/lib/schema-registry/schemas/caravan.ts
  - src/lib/schema-registry/schemas/general-goods.ts
autonomous: true
requirements:
  - SUBTYPE-04
  - SUBTYPE-05
  - SUBTYPE-06
  - SUBTYPE-08

must_haves:
  truths:
    - "Agriculture subtype selector appears for the first time with 12 options"
    - "Forklift subtype selector appears for the first time with 9 options including Telehandler"
    - "Caravan subtype selector appears for the first time with 5 options including Motorhome (no space)"
    - "General Goods subtype selector shows 16 options replacing the old 5-item list"
  artifacts:
    - path: "src/lib/schema-registry/schemas/agriculture.ts"
      provides: "Agriculture subtypes array — 12 entries"
      contains: "mower_conditioner"
    - path: "src/lib/schema-registry/schemas/forklift.ts"
      provides: "Forklift subtypes array — 9 entries"
      contains: "telehandler"
    - path: "src/lib/schema-registry/schemas/caravan.ts"
      provides: "Caravan subtypes array — 5 entries"
      contains: "motorhome"
    - path: "src/lib/schema-registry/schemas/general-goods.ts"
      provides: "General Goods subtypes array — 16 entries"
      contains: "jewellery_watches_collectables"
  key_links:
    - from: "src/lib/schema-registry/schemas/agriculture.ts"
      to: "wizard Step 3 AssetSubtypeSelector"
      via: "getSubtypes('agriculture') — subtypes.length > 0 gate in wizard now passes"
      pattern: "air_seeder"
    - from: "src/lib/schema-registry/schemas/forklift.ts"
      to: "wizard Step 3 AssetSubtypeSelector"
      via: "getSubtypes('forklift') — subtypes.length > 0 gate in wizard now passes"
      pattern: "telehandler"
---

<objective>
Replace the `subtypes` arrays in four schema files — Agriculture, Forklift, Caravan, and General Goods — with the exact Salesforce-matching arrays defined in CONTEXT.md. Agriculture, Forklift, and Caravan gain new subtype lists for the first time (their wizard Step 3 selector will auto-appear once these arrays are populated). No components, registry files, or field definitions change.

Purpose: Agriculture/Forklift/Caravan currently have stale placeholder subtypes and no working selector in the wizard; General Goods has the wrong 5-item list. After this plan all four types have correct Salesforce-aligned subtypes.
Output: Four updated schema files. No new files created.
</objective>

<execution_context>
@/home/jack/.claude/get-shit-done/workflows/execute-plan.md
@/home/jack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-subtype-schema-alignment/16-CONTEXT.md
@.planning/phases/16-subtype-schema-alignment/16-RESEARCH.md

<interfaces>
<!-- AssetSubtype shape — from src/lib/schema-registry/types.ts -->
```typescript
export type AssetSubtype = {
  key: string    // snake_case internal key stored in DB
  label: string  // display label — must match Salesforce verbatim
}
```

<!-- UI wiring note: Agriculture, Forklift, and Caravan selectors are NOT blocked by any conditional
     guard in the wizard. The wizard's Step 3 gates on `subtypes.length > 0`. Populating the array
     is the ONLY change needed for the selector to appear — no component changes required. -->

<!-- Caravan displayName is "Caravan / Motor Home" — DO NOT change this value. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Agriculture and Forklift subtype arrays</name>
  <files>
    src/lib/schema-registry/schemas/agriculture.ts
    src/lib/schema-registry/schemas/forklift.ts
  </files>
  <read_first>
    - src/lib/schema-registry/schemas/agriculture.ts — current subtypes array (6 entries: tractor, header, sprayer, planter, baler, cultivation) to be replaced
    - src/lib/schema-registry/schemas/forklift.ts — current subtypes array (4 entries: counterbalance, reach_truck, order_picker, telehandler) to be replaced
    - .planning/phases/16-subtype-schema-alignment/16-CONTEXT.md — locked key/label lists (sections: "Agriculture subtypes" and "Forklift subtypes")
  </read_first>
  <action>
In `agriculture.ts`, replace the entire `subtypes: [...]` block with these 12 entries in alphabetical-by-label order. Touch ONLY the subtypes array — leave all fields, hasGlassValuation, displayName, assetType, and descriptionTemplate exactly as-is. The old 6 subtypes (tractor, header, sprayer, planter, baler, cultivation) are fully replaced.

```typescript
subtypes: [
  { key: 'air_seeder',        label: 'Air Seeder' },
  { key: 'baler',             label: 'Baler' },
  { key: 'combine_harvester', label: 'Combine Harvester' },
  { key: 'coupe',             label: 'Coupe' },
  { key: 'disc_seeder',       label: 'Disc Seeder' },
  { key: 'forestry',          label: 'Forestry' },
  { key: 'grain_auger',       label: 'Grain Auger' },
  { key: 'mower_conditioner', label: 'Mower/Conditioner' },
  { key: 'other',             label: 'Other' },
  { key: 'plough',            label: 'Plough' },
  { key: 'spray_rig',         label: 'Spray Rig' },
  { key: 'tractor',           label: 'Tractor' },
],
```

Key rules:
- Label "Mower/Conditioner" must include the slash with no surrounding spaces
- Label "Combine Harvester" must be two words (not "Combine-Harvester")
- `coupe` is a Salesforce-required entry — include it
- Old keys `header`, `sprayer`, `planter`, `cultivation` must not remain

In `forklift.ts`, replace the entire `subtypes: [...]` block with these 9 entries in alphabetical-by-label order. The old 4 subtypes (counterbalance, reach_truck, order_picker, telehandler) are fully replaced — telehandler is kept but all other old entries are removed.

```typescript
subtypes: [
  { key: 'clearview_mast',      label: 'Clearview Mast' },
  { key: 'container_mast',      label: 'Container Mast' },
  { key: 'electric_pallet_jack', label: 'Electric Pallet Jack' },
  { key: 'ewp',                 label: 'EWP' },
  { key: 'other',               label: 'Other' },
  { key: 'stock_picker',        label: 'Stock Picker' },
  { key: 'telehandler',         label: 'Telehandler' },
  { key: 'walk_behind',         label: 'Walk Behind' },
  { key: 'walkie_stacker',      label: 'Walkie Stacker' },
],
```

Key rules:
- `counterbalance`, `reach_truck`, `order_picker` must not remain (old SF-incorrect keys removed)
- `telehandler` stays — it moved from Earthmoving to Forklift; it exists in both schemas during transition (earthmoving removes it in Plan 01)
- `ewp` appears here AND in the truck schema — this is intentional (forklift-mounted EWPs vs truck-body EWPs)
  </action>
  <verify>
    <automated>cd /home/jack/projects/prestige_assets && grep "mower_conditioner" src/lib/schema-registry/schemas/agriculture.ts && grep "electric_pallet_jack" src/lib/schema-registry/schemas/forklift.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/schema-registry/schemas/agriculture.ts contains `mower_conditioner`
    - src/lib/schema-registry/schemas/agriculture.ts contains `'Mower/Conditioner'` (slash, no surrounding spaces)
    - src/lib/schema-registry/schemas/agriculture.ts contains `combine_harvester`
    - src/lib/schema-registry/schemas/agriculture.ts contains `coupe`
    - src/lib/schema-registry/schemas/agriculture.ts does NOT contain `{ key: 'header'`
    - src/lib/schema-registry/schemas/agriculture.ts does NOT contain `{ key: 'sprayer'`
    - src/lib/schema-registry/schemas/agriculture.ts does NOT contain `{ key: 'planter'`
    - src/lib/schema-registry/schemas/agriculture.ts does NOT contain `{ key: 'cultivation'`
    - Count of `{ key:` entries in the subtypes block of agriculture.ts = 12
    - src/lib/schema-registry/schemas/forklift.ts contains `electric_pallet_jack`
    - src/lib/schema-registry/schemas/forklift.ts contains `walkie_stacker`
    - src/lib/schema-registry/schemas/forklift.ts contains `telehandler`
    - src/lib/schema-registry/schemas/forklift.ts does NOT contain `counterbalance`
    - src/lib/schema-registry/schemas/forklift.ts does NOT contain `reach_truck`
    - src/lib/schema-registry/schemas/forklift.ts does NOT contain `order_picker`
    - Count of `{ key:` entries in the subtypes block of forklift.ts = 9
  </acceptance_criteria>
  <done>agriculture.ts has 12 subtypes (full replacement of old 6). forklift.ts has 9 subtypes (telehandler retained, 3 old keys removed, 6 new SF keys added).</done>
</task>

<task type="auto">
  <name>Task 2: Replace Caravan and General Goods subtype arrays</name>
  <files>
    src/lib/schema-registry/schemas/caravan.ts
    src/lib/schema-registry/schemas/general-goods.ts
  </files>
  <read_first>
    - src/lib/schema-registry/schemas/caravan.ts — current subtypes array (3 entries: caravan, motor_home, camper_trailer) to be replaced; also note `displayName: 'Caravan / Motor Home'` — DO NOT change this
    - src/lib/schema-registry/schemas/general-goods.ts — current subtypes array (5 entries: tools_equipment, attachments, workshop_equipment, office_it, miscellaneous) to be replaced
    - .planning/phases/16-subtype-schema-alignment/16-CONTEXT.md — locked key/label lists (sections: "Caravan subtypes" and "General Goods subtypes")
  </read_first>
  <action>
In `caravan.ts`, replace the entire `subtypes: [...]` block with these 5 entries in alphabetical-by-label order. Touch ONLY the subtypes array — leave `displayName: 'Caravan / Motor Home'` and all other properties exactly as-is.

```typescript
subtypes: [
  { key: 'camper_trailer', label: 'Camper Trailer' },
  { key: 'caravan',        label: 'Caravan' },
  { key: 'coupe',          label: 'Coupe' },
  { key: 'motorhome',      label: 'Motorhome' },
  { key: 'other',          label: 'Other' },
],
```

Key rules:
- `motor_home` (old key, two words) must not remain — replaced by `motorhome` (no space)
- Label must be "Motorhome" (one word, no space) — matches Salesforce exactly
- `coupe` is a Salesforce-required entry — include it
- `camper_trailer` and `caravan` are carried over from old array (keys unchanged)
- `displayName` stays as "Caravan / Motor Home" — do NOT change it

In `general-goods.ts`, replace the entire `subtypes: [...]` block with these 16 entries in alphabetical-by-label order. The old 5 subtypes (tools_equipment, attachments, workshop_equipment, office_it, miscellaneous) are fully replaced.

```typescript
subtypes: [
  { key: 'agriculture',                   label: 'Agriculture' },
  { key: 'gardening_landscaping',         label: 'Gardening & Landscaping' },
  { key: 'goodwill',                      label: 'Goodwill' },
  { key: 'health_fitness',                label: 'Health & Fitness' },
  { key: 'hospitality',                   label: 'Hospitality' },
  { key: 'it_computers',                  label: 'IT & Computers' },
  { key: 'jewellery_watches_collectables', label: 'Jewellery/Watches/Collectables' },
  { key: 'medical',                       label: 'Medical' },
  { key: 'miscellaneous',                 label: 'Miscellaneous' },
  { key: 'office',                        label: 'Office' },
  { key: 'other',                         label: 'Other' },
  { key: 'plant_equipment',               label: 'Plant & Equipment' },
  { key: 'retail_fit_out',                label: 'Retail Fit Out' },
  { key: 'retail_stock',                  label: 'Retail Stock' },
  { key: 'signage',                       label: 'Signage' },
  { key: 'tools_toolboxes',               label: 'Tools & Toolboxes' },
],
```

Key rules:
- Label "Gardening & Landscaping" must include the ampersand (not "and")
- Label "Health & Fitness" must include the ampersand
- Label "IT & Computers" must include the ampersand
- Label "Jewellery/Watches/Collectables" must include slashes with no surrounding spaces
- Label "Plant & Equipment" must include the ampersand
- Label "Tools & Toolboxes" must include the ampersand
- Old keys `tools_equipment`, `attachments`, `workshop_equipment`, `office_it` must not remain
  </action>
  <verify>
    <automated>cd /home/jack/projects/prestige_assets && grep "motorhome" src/lib/schema-registry/schemas/caravan.ts && grep "jewellery_watches_collectables" src/lib/schema-registry/schemas/general-goods.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/schema-registry/schemas/caravan.ts contains `motorhome` (no space in key)
    - src/lib/schema-registry/schemas/caravan.ts contains `'Motorhome'` (no space in label)
    - src/lib/schema-registry/schemas/caravan.ts does NOT contain `motor_home`
    - src/lib/schema-registry/schemas/caravan.ts contains `coupe`
    - src/lib/schema-registry/schemas/caravan.ts still contains `displayName: 'Caravan / Motor Home'` (unchanged)
    - Count of `{ key:` entries in the subtypes block of caravan.ts = 5
    - src/lib/schema-registry/schemas/general-goods.ts contains `jewellery_watches_collectables`
    - src/lib/schema-registry/schemas/general-goods.ts contains `'Jewellery/Watches/Collectables'`
    - src/lib/schema-registry/schemas/general-goods.ts contains `gardening_landscaping`
    - src/lib/schema-registry/schemas/general-goods.ts contains `'Gardening & Landscaping'`
    - src/lib/schema-registry/schemas/general-goods.ts contains `tools_toolboxes`
    - src/lib/schema-registry/schemas/general-goods.ts does NOT contain `tools_equipment`
    - src/lib/schema-registry/schemas/general-goods.ts does NOT contain `workshop_equipment`
    - src/lib/schema-registry/schemas/general-goods.ts does NOT contain `office_it`
    - src/lib/schema-registry/schemas/general-goods.ts does NOT contain `{ key: 'attachments'` (old general-goods key — note earthmoving has an attachments key legitimately)
    - Count of `{ key:` entries in the subtypes block of general-goods.ts = 16
  </acceptance_criteria>
  <done>caravan.ts has 5 subtypes (motorhome replaces motor_home, coupe and other added). general-goods.ts has 16 subtypes replacing old 5, all labels match Salesforce verbatim including ampersands and slashes.</done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
cd /home/jack/projects/prestige_assets
grep "motorhome" src/lib/schema-registry/schemas/caravan.ts
# Must match — key and label without space

grep "motor_home" src/lib/schema-registry/schemas/caravan.ts
# Must return nothing

grep "jewellery_watches_collectables" src/lib/schema-registry/schemas/general-goods.ts
# Must match

grep "Gardening & Landscaping" src/lib/schema-registry/schemas/general-goods.ts
# Must match with ampersand
```

Note: The test suite (`npm test`) will fail after this plan because `schema-registry.test.ts` still has old assertions (`toHaveLength(5)` for general_goods, etc.). That is expected — Plan 03 updates the tests.
</verification>

<success_criteria>
- agriculture.ts subtypes array: 12 entries, old keys (header, sprayer, planter, cultivation) absent, mower_conditioner and combine_harvester present with correct labels
- forklift.ts subtypes array: 9 entries, old keys (counterbalance, reach_truck, order_picker) absent, telehandler and electric_pallet_jack present
- caravan.ts subtypes array: 5 entries, motor_home absent, motorhome present with "Motorhome" label, displayName unchanged
- general-goods.ts subtypes array: 16 entries, old keys absent, all new entries with exact SF labels including ampersands and slashes
- No other properties changed in any of the 4 files
</success_criteria>

<output>
After completion, create `.planning/phases/16-subtype-schema-alignment/16-02-SUMMARY.md`
</output>
