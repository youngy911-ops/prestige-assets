---
plan: 23-01
phase: 23
title: Code Quality & Accessibility
wave: 1
autonomous: true
requirements: [CODE-01, CODE-02, A11Y-01, A11Y-02, A11Y-03]
files_modified:
  - src/lib/constants/storage-keys.ts
  - src/app/(app)/assets/new/page.tsx
  - src/app/(app)/assets/quick/page.tsx
  - src/components/asset/HomePageClient.tsx
  - src/components/asset/AssetList.tsx
  - src/components/nav/BottomNav.tsx
  - src/components/asset/ExtractionResultPanel.tsx
  - src/components/asset/AssetCard.tsx
  - src/components/asset/PhotoThumbnail.tsx
---

<objective>
Deduplicate LAST_BRANCH_KEY into a shared constants file, and add ARIA attributes to BottomNav active links, the ExtractionResultPanel expandable section, and asset card images.

Note: ExtractionLoadingState already uses Lucide icons (not emoji) — success criterion 2 is already met.
</objective>

<must_haves>
- LAST_BRANCH_KEY defined once in src/lib/constants/storage-keys.ts and imported by all 4 consumers
- BottomNav active links have aria-current="page"
- ExtractionResultPanel "not found" toggle has aria-expanded={showNotFound}
- AssetCard thumbnail has meaningful alt text or role="presentation"
- PhotoThumbnail decorative image has role="presentation" (alt="" already present, add role)
</must_haves>

<tasks>

<task id="1" name="Create shared storage-keys constants file">
Create `src/lib/constants/storage-keys.ts`:
```ts
export const LAST_BRANCH_KEY = 'lastUsedBranch'
```

Then update all 4 consumers to import from this file instead of defining their own:
- src/app/(app)/assets/new/page.tsx — remove `const LAST_BRANCH_KEY = 'lastUsedBranch'`, add import
- src/app/(app)/assets/quick/page.tsx — same
- src/components/asset/HomePageClient.tsx — same
- src/components/asset/AssetList.tsx — same

Commit: `refactor(CODE-01): deduplicate LAST_BRANCH_KEY into shared constants`
</task>

<task id="2" name="Add aria-current to BottomNav active links">
In `src/components/nav/BottomNav.tsx`, add `aria-current="page"` to each Link when its active condition is true:
- Assets link: `aria-current={assetsActive ? 'page' : undefined}`
- New Asset link: `aria-current={newActive ? 'page' : undefined}`
- Quick link: `aria-current={quickActive ? 'page' : undefined}`

Commit: `feat(A11Y-01): add aria-current="page" to active BottomNav links`
</task>

<task id="3" name="Add aria-expanded to ExtractionResultPanel toggle">
In `src/components/asset/ExtractionResultPanel.tsx`, find the button that toggles `showNotFound` and add `aria-expanded={showNotFound}`.

Commit: `feat(A11Y-02): add aria-expanded to not-found fields toggle`
</task>

<task id="4" name="Fix image alt text on AssetCard and PhotoThumbnail">
In `src/components/asset/AssetCard.tsx`:
- The thumbnail `<img>` has `alt=""` — change to `alt={[asset_subtype, asset_type].filter(Boolean).join(' ') || 'Asset thumbnail'}` using props already available, or add `role="presentation"` if it's purely decorative (it has a fallback icon so the image is decorative when it loads).
- Add `role="presentation"` alongside `alt=""` since the icon fallback conveys the same info.

In `src/components/asset/PhotoThumbnail.tsx`:
- Already has `alt=""` — add `role="presentation"` to be explicit about decorative intent.

Commit: `feat(A11Y-03): mark decorative asset thumbnails as presentation role`
</task>

</tasks>

<self_check>
- [ ] `grep -rn "LAST_BRANCH_KEY = " src/` returns only storage-keys.ts
- [ ] `grep -n "aria-current" src/components/nav/BottomNav.tsx` shows 3 matches
- [ ] `grep -n "aria-expanded" src/components/asset/ExtractionResultPanel.tsx` shows 1 match
- [ ] `grep -n "role=\"presentation\"" src/components/asset/AssetCard.tsx` shows 1 match
- [ ] `npx tsc --noEmit` passes
</self_check>
