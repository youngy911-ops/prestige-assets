---
phase: 06.1-ai-extraction-quality
plan: 01
subsystem: api
tags: [zod, schema-registry, ai-extraction, typescript]

# Dependency graph
requires:
  - phase: 03-ai-extraction
    provides: buildExtractionSchema, buildSystemPrompt, getAIExtractableFields
  - phase: 01-foundation
    provides: FieldDefinition type, SCHEMA_REGISTRY, asset schemas
provides:
  - aiHint?: string property on FieldDefinition type
  - getAIExtractableFieldDefs() returning FieldDefinition[] from schema registry
  - buildExtractionSchema composing label + hint + options into .describe() strings
  - buildSystemPrompt with explicit plate-type routing (BUILD PLATE, COMPLIANCE PLATE, INSTRUMENT CLUSTER)
  - Convention enforcement test (RED) requiring all aiExtractable non-textarea fields to have aiHint
affects:
  - 06.1-02-PLAN (truck schema aiHints)
  - 06.1-03-PLAN (earthmoving/agriculture/forklift schema aiHints)
  - 06.1-04-PLAN (trailer/caravan/general_goods schema aiHints)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "FieldDefinition.aiHint?: string — per-field GPT guidance embedded into Zod .describe() strings"
    - "getAIExtractableFieldDefs() returns FieldDefinition[] instead of string[] for downstream schema builders"
    - "buildExtractionSchema composes: Salesforce label + aiHint + options list per field"
    - "Convention enforcement test (intentionally RED) as contract for Plans 02-04 to satisfy"

key-files:
  created: []
  modified:
    - src/lib/schema-registry/types.ts
    - src/lib/schema-registry/index.ts
    - src/lib/ai/extraction-schema.ts
    - src/__tests__/extraction-schema.test.ts
    - src/__tests__/schema-registry.test.ts

key-decisions:
  - "getAIExtractableFields() (string[]) kept unchanged — getAIExtractableFieldDefs() added alongside it to avoid breaking callers"
  - "Convention enforcement test intentionally RED — establishes contract for Plans 02-04; aiHint not back-filled into schemas in this plan"
  - "buildSystemPrompt Step 1 replaced with explicit plate-type list (BUILD PLATE, COMPLIANCE PLATE, INSTRUMENT CLUSTER, etc.) — Step 2 replaced with per-asset-type inference lists"
  - "Anti-fabrication rule strengthened: explicit prohibition on serial numbers, VINs, engine numbers, PIN numbers even via inference"

patterns-established:
  - "aiHint convention: aiExtractable: true + inputType !== textarea => aiHint required (enforced by test)"
  - "Rich Zod describe strings: Salesforce field label + aiHint + options list, all composed in buildExtractionSchema"

requirements-completed: []

# Metrics
duration: 12min
completed: 2026-03-21
---

# Phase 06.1 Plan 01: AI Extraction Foundation Summary

**aiHint added to FieldDefinition, getAIExtractableFieldDefs() returning FieldDefinition[], buildExtractionSchema composing rich label+hint+options .describe() strings, and buildSystemPrompt with explicit plate-type routing**

## Performance

- **Duration:** 12 min
- **Started:** 2026-03-21T02:08:00Z
- **Completed:** 2026-03-21T02:20:38Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- Added `aiHint?: string` to `FieldDefinition` type — foundational property for Plans 02-04 to populate
- Added `getAIExtractableFieldDefs()` to schema registry, returning `FieldDefinition[]` rather than `string[]`
- Updated `buildExtractionSchema` to compose per-field descriptions: `Salesforce field: "{label}". {aiHint} Must be exactly one of: {options}. Return null if not determinable.`
- Updated `buildSystemPrompt` Steps 1 and 2 with explicit plate-type routing and per-asset-class inference lists
- Added 10 new passing tests (getAIExtractableFieldDefs, rich descriptions, plate routing) plus 1 intentionally RED convention enforcement test

## Task Commits

Each task was committed atomically:

1. **Task 1: Add aiHint, getAIExtractableFieldDefs, update buildExtractionSchema and buildSystemPrompt** - `bcbe8d6` (feat)
2. **Task 2: Add test coverage for new behaviors** - `b969b0c` (test)

_Note: TDD tasks — implementation and tests committed in separate commits_

## Files Created/Modified
- `src/lib/schema-registry/types.ts` - Added `aiHint?: string` to FieldDefinition after `aiExtractable: boolean`
- `src/lib/schema-registry/index.ts` - Added `getAIExtractableFieldDefs()` alongside existing `getAIExtractableFields()`
- `src/lib/ai/extraction-schema.ts` - Updated import, rewrote `buildExtractionSchema` loop, replaced `buildSystemPrompt` Steps 1 and 2
- `src/__tests__/extraction-schema.test.ts` - Appended 3 new describe blocks (10 tests), all GREEN
- `src/__tests__/schema-registry.test.ts` - Appended `aiHint convention enforcement` describe block (1 test, intentionally RED)

## Decisions Made
- `getAIExtractableFields()` (returns `string[]`) was NOT changed — `getAIExtractableFieldDefs()` added alongside it to avoid breaking existing callers like the existing tests
- Convention enforcement test intentionally RED: establishes the contract for Plans 02-04 to satisfy by adding `aiHint` to all aiExtractable non-textarea fields in each schema
- `buildSystemPrompt` Step 1 replaced with an explicit ordered list of plate types; Step 2 replaced with per-asset-class inference lists (TRUCKS, EARTHMOVING, FORKLIFTS, AGRICULTURE)
- Anti-fabrication rule strengthened: added explicit prohibition on fabricating serial numbers, VINs, engine numbers, PIN numbers

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Plans 02, 03, 04 can now add `aiHint` to each asset schema's `aiExtractable: true` non-textarea fields
- When all schemas have aiHints, the convention enforcement test will turn GREEN automatically
- `buildExtractionSchema` will compose the hints into AI prompts as soon as they are present in the schema definitions

## Self-Check: PASSED

All files confirmed on disk. All task commits verified in git log.

---
*Phase: 06.1-ai-extraction-quality*
*Completed: 2026-03-21*
