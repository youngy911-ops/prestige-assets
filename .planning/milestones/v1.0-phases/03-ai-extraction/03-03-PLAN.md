---
phase: 03-ai-extraction
plan: "03"
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - src/__tests__/extraction-schema.test.ts
  - src/__tests__/extract-route.test.ts
  - src/app/api/extract/route.ts
autonomous: true
gap_closure: true
requirements: [AI-01, AI-02, AI-03]

must_haves:
  truths:
    - "npm run test -- --run exits 0 with no failures in extraction-schema.test.ts or extract-route.test.ts"
    - "The truck priority fields test documents 4 fields (post-UAT reality), not 5"
    - "The extract route success test asserts the actual response shape { success: true, extraction_result: ... }"
    - "route.ts imports only what it uses — getInspectionPriorityFields call and import are removed"
  artifacts:
    - path: "src/__tests__/extraction-schema.test.ts"
      provides: "Correct truck priority field assertions (4 fields, no registration_expiry)"
    - path: "src/__tests__/extract-route.test.ts"
      provides: "Correct success-path response shape assertion"
    - path: "src/app/api/extract/route.ts"
      provides: "No dead import or void call"
  key_links:
    - from: "src/__tests__/extraction-schema.test.ts"
      to: "src/lib/schema-registry/index.ts"
      via: "getInspectionPriorityFields('truck') — now returns 4 fields"
      pattern: "toHaveLength\\(4\\)"
    - from: "src/__tests__/extract-route.test.ts"
      to: "src/app/api/extract/route.ts"
      via: "POST response body shape"
      pattern: "extraction_result.*expect\\.any\\(Object\\)"
---

<objective>
Align the test suite with UAT-driven codebase changes and remove dead code introduced during that fix.

Purpose: Three test failures and one dead-code smell were left behind when the UAT fix (a) removed registration_expiry from the truck schema and (b) added extraction_result to the route response body. The production code is correct; only the test contracts and one unused import need updating.
Output: All vitest tests pass (npm run test -- --run exits 0). route.ts has no unused import or void call.
</objective>

<execution_context>
@/home/jack/.claude/get-shit-done/workflows/execute-plan.md
@/home/jack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix stale test assertions in extraction-schema.test.ts and extract-route.test.ts</name>
  <files>src/__tests__/extraction-schema.test.ts, src/__tests__/extract-route.test.ts</files>
  <action>
Two test files have assertions that were not updated after the UAT fix. Make the following targeted changes only — do not alter any other test logic.

**src/__tests__/extraction-schema.test.ts — two changes:**

1. Line 7: Update the it() description and the toEqual assertion to reflect the 4-field reality (registration_expiry removed during UAT).
   - Old description: `'truck returns [odometer, registration_number, registration_expiry, hourmeter, service_history] sorted by sfOrder'`
   - New description: `'truck returns [odometer, registration_number, hourmeter, service_history] sorted by sfOrder'`
   - Old comment on line 8: `// sfOrders: odometer=17, registration_number=18, registration_expiry=19, hourmeter=22, service_history=32`
   - New comment: `// sfOrders: odometer=17, registration_number=18, hourmeter=22, service_history=32`
   - Old toEqual on line 10: `expect(fields).toEqual(['odometer', 'registration_number', 'registration_expiry', 'hourmeter', 'service_history'])`
   - New toEqual: `expect(fields).toEqual(['odometer', 'registration_number', 'hourmeter', 'service_history'])`

2. Line 48-50: Update the length assertion.
   - Old: `it('truck returns exactly 5 priority fields', () => {`  and  `expect(getInspectionPriorityFields('truck')).toHaveLength(5)`
   - New: `it('truck returns exactly 4 priority fields', () => {`  and  `expect(getInspectionPriorityFields('truck')).toHaveLength(4)`

**src/__tests__/extract-route.test.ts — one change:**

Line 123: Update the success-path body assertion to match the actual route response shape.
   - Old: `expect(body).toEqual({ success: true })`
   - New: `expect(body).toEqual({ success: true, extraction_result: expect.any(Object) })`

No other lines in either file should be changed.
  </action>
  <verify>
    <automated>cd /home/jack/projects/prestige_assets && npm run test -- --run src/__tests__/extraction-schema.test.ts src/__tests__/extract-route.test.ts</automated>
  </verify>
  <done>All tests in both files pass. No references to registration_expiry remain in extraction-schema.test.ts. extract-route.test.ts success path asserts extraction_result.</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead getInspectionPriorityFields import and void call from route.ts</name>
  <files>src/app/api/extract/route.ts</files>
  <action>
The route handler imports getInspectionPriorityFields and calls it with void to suppress a lint warning, but the return value is never used. This is dead code left over from a plan decision that was later simplified. Remove it cleanly.

**src/app/api/extract/route.ts — two changes:**

1. Line 6: Remove the import entirely.
   - Remove: `import { getInspectionPriorityFields } from '@/lib/schema-registry'`

2. Lines 58-62: Remove the dead call and its accompanying comments. The block to remove is:
   ```
   // Priority fields are for UI display — their values come through inspection_notes text
   // No separate structured fields parsing needed; inspection_notes is plain text from staff
   const structuredFields: Record<string, string> = {}
   // Suppress unused variable warning
   void getInspectionPriorityFields(assetType)
   ```
   Replace with just:
   ```
   const structuredFields: Record<string, string> = {}
   ```
   (The structuredFields variable is still used on line 64 in buildUserPrompt — keep that declaration.)

Do not change any other lines. The route logic, auth check, DB calls, and response shape are all correct and must remain untouched.
  </action>
  <verify>
    <automated>cd /home/jack/projects/prestige_assets && npm run test -- --run src/__tests__/extract-route.test.ts && grep -n "getInspectionPriorityFields" src/app/api/extract/route.ts; echo "exit:$?"</automated>
  </verify>
  <done>route.ts contains no reference to getInspectionPriorityFields (grep returns no matches). extract-route tests still pass. TypeScript build has no new errors.</done>
</task>

</tasks>

<verification>
Run the full test suite to confirm no regressions:

```
cd /home/jack/projects/prestige_assets && npm run test -- --run
```

Expected: all tests pass, exit 0.

Also confirm no TypeScript errors introduced:

```
cd /home/jack/projects/prestige_assets && npx tsc --noEmit
```
</verification>

<success_criteria>
- npm run test -- --run exits 0 with zero failures
- extraction-schema.test.ts truck assertions document 4 fields, registration_expiry absent
- extract-route.test.ts success path asserts { success: true, extraction_result: expect.any(Object) }
- route.ts has no import or reference to getInspectionPriorityFields
- No TypeScript errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-extraction/03-03-SUMMARY.md` following the summary template.
</output>
