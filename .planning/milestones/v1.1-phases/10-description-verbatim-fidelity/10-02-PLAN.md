---
phase: 10-description-verbatim-fidelity
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified: []
autonomous: false
requirements:
  - DESCR-01

must_haves:
  truths:
    - "A truck asset with inspection_notes containing 'Notes: 48\" sleeper cab' produces a GPT-4o description that contains '48\" sleeper cab' verbatim"
    - "Structured key:value lines from inspection_notes (e.g. Suspension Type: Airbag) appear in the description or are not incorrectly paraphrased"
  artifacts:
    - path: "src/app/api/describe/route.ts"
      provides: "Deployed route with verbatim prompt changes from plan 01"
      contains: "Staff-provided values (use verbatim)"
  key_links:
    - from: "describe/route.ts buildDescriptionUserPrompt"
      to: "GPT-4o user message"
      via: "Staff-provided values block"
      pattern: "Staff-provided values (use verbatim)"
---

<objective>
Human verification that the live GPT-4o describe endpoint produces descriptions with specific values verbatim — not paraphrased.

Purpose: Unit tests prove the prompt structure is correct; this checkpoint proves GPT-4o actually honours the instruction at runtime.
Output: Confirmed pass/fail on DESCR-01 in production conditions.
</objective>

<execution_context>
@/home/jack/.claude/get-shit-done/workflows/execute-plan.md
@/home/jack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-description-verbatim-fidelity/10-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify GPT-4o description contains verbatim values from inspection notes</name>
  <files>none</files>
  <action>No code changes. This task verifies that plan 01's prompt changes cause GPT-4o to output verbatim values at runtime. Follow the how-to-verify steps.</action>
  <what-built>
    Plan 01 added:
    - A new UNIVERSAL RULES bullet to DESCRIPTION_SYSTEM_PROMPT: "Values and measurements from inspection notes must appear verbatim in the description — do not paraphrase, convert units, or interpret. If notes say '48" sleeper cab', write '48" sleeper cab'"
    - Restructured buildDescriptionUserPrompt to split inspection_notes into two blocks: "Staff-provided values (use verbatim):" for key:value lines and "Inspection notes:" for freeform content
  </what-built>
  <how-to-verify>
    1. Open the app in the browser (dev: `npm run dev` if not already running)

    2. Create or open a truck asset. In the Inspection Notes section, enter the following values before running extraction:
       - Set Suspension Type to a value (e.g. "Airbag")
       - In the free-text Notes field, enter: `48" sleeper cab`
       (The inspection_notes field will serialise as: `Suspension Type: Airbag\nNotes: 48" sleeper cab`)

    3. Run AI Extraction (to populate Salesforce fields), then navigate to the Description tab and click "Generate Description".

    4. Inspect the generated description text. Verify:
       - The text `48"` appears in the description (not stripped to just `sleeper cab`)
       - The value is written as `48" sleeper cab` — not paraphrased

    5. (Optional, stronger test) Try with a measurement that GPT is likely to paraphrase — e.g. Notes: `Double sleeper cab, 48" bunk` — confirm `48"` survives.

    Acceptable pass criteria:
    - `48"` appears verbatim in the description output at least once
    - No measurement from inspection notes is silently dropped or paraphrased to a unit-free equivalent

    If the description does NOT contain the verbatim value, describe what appeared instead — this indicates the prompt changes may need strengthening.
  </how-to-verify>
  <verify>
    <automated>MISSING — runtime GPT-4o output cannot be verified automatically; manual verification required per VALIDATION.md</automated>
  </verify>
  <done>Human confirms `48"` (or equivalent entered measurement) appears verbatim in the generated description output.</done>
  <resume-signal>Type "approved" if 48" appears verbatim in the description, or describe the actual output if it was paraphrased</resume-signal>
</task>

</tasks>

<verification>
Human confirms: specific values from inspection notes appear verbatim in GPT-4o generated description output.
</verification>

<success_criteria>
- DESCR-01: AI-generated description contains `48"` (or the exact measurement entered) — not paraphrased
- Human approves checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/10-description-verbatim-fidelity/10-02-SUMMARY.md`
</output>
